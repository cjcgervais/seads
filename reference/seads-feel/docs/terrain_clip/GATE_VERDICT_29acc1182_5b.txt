# GATE VERDICT -- terrain-clip, tip 29acc1182, SOP 5b FROM CLEAN, GATE ALONE
# THE POST-MERGE GATE. This is the first gate this lane has run on a tree that
# CONTAINS main. Every earlier verdict on this lane (a18fc9b1c, dba79e46b) was
# run on a PRE-MERGE tree and its count (2130) is VOID -- main moved with code.
#
# Trimmed to header + verdict + the red set, per the lane precedent
# (docs/road_repair/GATE_VERDICT_b659a119d.txt): the raw ctest log is NOT
# committed for repo weight. THE LOG LIVES AT build/gate_29acc1182.log IN THIS
# WORKTREE, UNTRACKED (.gitignore:1 "build/") -- the sentinel's rule; it is NOT
# in a scratchpad, so an auditor on this box can read the raw run.
#
# Tree:   sandbox/terrain-clip @ 29acc1182 (the landing docs) over f7ccbfa30
#         (the merge of origin/main ed0a43ce8, the road-repair landing) over
#         b58a8a52d. The CODE is f7ccbfa30; 29acc1182 adds only docs.
#         The lane tip is now fc02f8873 (his tag ruling + his recon word), which
#         is ALSO docs-only: "git diff --stat 29acc1182 fc02f8873 -- sim control
#         app render world test config CMakeLists.txt" prints NOTHING, so this
#         verdict describes the tip's code exactly, not approximately.
#
# 5b(1) RECONFIGURE FROM CLEAN. build/CMakeCache.txt and build/CMakeFiles/ were
#         DELETED, then reconfigured with the generator/compilers/flags read off
#         the old cache before deleting it (a copy was kept aside):
#           cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug \
#             -DCMAKE_C_COMPILER=<winlibs mingw64>/bin/gcc.exe \
#             -DCMAKE_CXX_COMPILER=<winlibs mingw64>/bin/c++.exe \
#             -DCMAKE_MAKE_PROGRAM=<winlibs mingw64>/bin/ninja.exe \
#             -DFETCHCONTENT_SOURCE_DIR_RAYLIB=D:/seads_sandboxes/cam-smooth/build/_deps/raylib-src
#         Generator Ninja, Debug, MinGW GCC (BrechtSanders WinLibs POSIX UCRT),
#         CMAKE_CXX_FLAGS empty, CMAKE_CXX_FLAGS_DEBUG=-g -- identical to the old
#         cache, and identical to the dba79e46b 5b run. build/_deps kept, no re-fetch.
# 5b(2) BUILD. "cmake --build build -j 8" -> 236/236, exit 0, both seads.exe and
#         seads_tests.exe relinked from this tip. No warnings-as-errors trip.
# 5b(3) COUNT. "ctest --test-dir build -C Debug -N | tail -1" -> Total Tests: 2137
#         = road-repair main ed0a43ce8's 2123 + 14. THE FOURTEEN, BY NAME AND BY
#         ctest NUMBER, all from this lane and no other:
#           #244  FACETCONTACT L1 dial: shipped 1.0, bounded [0,1], read by the app
#           #245  FACETCONTACT L1 identity BY BRANCH: at 0 the facet fn is not called
#           #246  FACETCONTACT L1 identity: a full ground_contact tick is unmoved at 0
#           #247  FACETCONTACT L2 an airframe inside the drawn ground is graded at 1.0
#           #248  FACETCONTACT L3 the clip census closes at dial 1 (raw and blurred)
#           #249  FACETCONTACT L4 the landing gap at the Sudbury spawn and the pump apron
#           #250  FACETCONTACT L5 cost: facet_radius_at per tick at 120 Hz
#           #251  FACETCONTACT L6 the drawn-above-facet residual, measured
#           #252  FACETCONTACT L7 building prisms are based on the contact surface
#           #253  FACETCONTACT L8 the prism base follows the facet through sim::step
#           #254  CLIPPROBE drawn-vs-crash surface at the reported sites
#           #255  CLIPPROBE cadence: the contact test is a POINT sample, not swept
#           #256  CLIPPROBE below-surface: what happens to an airframe under ground
#           #2013 TAPE T2b: facet_contact is stamped, read back, and mismatch warns
#         FACETCONTACT L6-STRICT ([.t3-strict]) and FACETAI ([.facetai]) are
#         HIDDEN, are NOT among the fourteen, and are deliberately not graded here.
#         14 is the SAME fourteen the pre-merge 5b run named: the merge added
#         main's +7 legs (2116 -> 2123) and MOVED THE SLED REDS' NUMBERS
#         (1287/1288/1326/1329 -> 1290/1291/1329/1332) but NOT their NAMES. That
#         is exactly the count-matched-while-the-members-moved failure that
#         gate_baseline.py exists to catch, and the reason the set below is
#         graded BY NAME by the runner and never by number by a human.
# 5b(4) RUN. "ctest --test-dir build -C Debug --output-on-failure", launched
#         DETACHED in a hidden process from this worktree, stdout to
#         build/gate_29acc1182.log. Before launch the box was swept for
#         ctest.exe / seads_tests / seads.exe. NO game was launched by this
#         session, NO build-play smoke ran during the gate, NO second ctest.
#
# (!) ONE DISCLOSURE ON THE BOX, so nobody finds it later and calls it hidden.
#   At launch, ONE process matched the sweep: seads.exe PID 14332, started
#   2026-09-17 21:26:18, image path
#   D:\seads_sandboxes\road-repair-f2\build-rel\seads.exe -- that is ANOTHER
#   LANE'S build-play and CHAD WAS PLAYING IT (he reported on recon at 21:19 and
#   was on the box across part of this window). It was not started by this
#   session and was not killed by it (memory "r4a-grip-release-rung": process
#   kills are scoped to a worktree; never a machine-wide kill). It had exited
#   before the gate finished: at 22:34 the sweep for seads / ctest / seads_tests
#   returned NOTHING but this gate's own, which had just ended.
#   NO other ctest.exe and NO other lane's GATE appeared at any poll (polled
#   every 30 s for the whole run). The contamination signature of the a18fc9b1c
#   run -- a second gate competing for the box -- is ABSENT. A GPU game sharing
#   the box can only inflate wall clock; no leg here grades wall clock, and the
#   red SET is what this file is evidence for. Wall clock 3904.41 s vs the solo
#   dba79e46b run's 3654.02 s: +250 s on +7 legs and a GPU game for part of it,
#   nowhere near the +498 s the genuinely box-shared a18fc9b1c run cost.
#
# CLOCKS, so nobody has to take "it was run on this tip" on faith:
#   merge f7ccbfa30 committed  2026-09-17T21:23:06-0700  (epoch 1789705386)
#   tip   29acc1182 committed  2026-09-17T21:25:43-0700  (epoch 1789705543)
#   gate launched              2026-09-17T21:28:59-0700  (epoch 1789705739, 196 s after the tip)
#   gate log last written      2026-09-17T22:34:04-0700  (epoch 1789709644)
#   total test time (real)     3904.41 s
#   => log mtime - launch = 3,905 s, which is the run's own 3904.41 s plus ~1 s of
#      ctest startup/teardown: the gate ran end to end inside the window that
#      opens at the launch, which itself opens AFTER the tip's commit. No part of
#      this gate predates the tree it claims to describe, and no part of it was
#      carried over from the dba79e46b run (different log, different file,
#      different count).
#   docs-only fc02f8873 was committed 2026-09-17T21:51:41-0700, DURING the run --
#      and it touches no compiled file, proved by the empty diff quoted above.
#      The gate's binaries were linked at 21:28 from 29acc1182 and nothing
#      relinked while the gate ran.
#   raw log (UNTRACKED, in this worktree): build/gate_29acc1182.log

=== THE RED SET ===

  79/2137 Test   #79: probe P-F: the relentless raider keeps the pump and shoots back ........................................................................................***Failed    0.54 sec
 119/2137 Test  #119: E12.1: the raider backfill keeps a faction's pump offense alive ........................................................................................***Failed  329.33 sec
1290/2137 Test #1290: sled_slides_before_it_tips_on_flat_snow ................................................................................................................***Failed    1.92 sec
1291/2137 Test #1291: sled_grip_ceiling_stays_below_the_tip_threshold ........................................................................................................***Failed   10.40 sec
1329/2137 Test #1329: sled_assist_reference_plane_is_load_weighted ...........................................................................................................***Failed    0.41 sec
1332/2137 Test #1332: sled_debug_sink_is_write_only ..........................................................................................................................***Failed    1.24 sec

=== ctest trailer (the failed list, by name) ===
99% tests passed, 6 tests failed out of 2137

Total Test time (real) = 3904.41 sec

The following tests FAILED:
	 79 - probe P-F: the relentless raider keeps the pump and shoots back (Failed)
	119 - E12.1: the raider backfill keeps a faction's pump offense alive (Failed)
	1290 - sled_slides_before_it_tips_on_flat_snow (Failed)
	1291 - sled_grip_ceiling_stays_below_the_tip_threshold (Failed)
	1329 - sled_assist_reference_plane_is_load_weighted (Failed)
	1332 - sled_debug_sink_is_write_only (Failed)

=== tools/gate/gate_baseline.py check (THE RUNNER WROTE THIS, not a hand grade) ===
gate: 6 failed of 2137
baseline: 6 known reds

OK -- the red set is EXACTLY the baseline, member for member.
GATE_EXIT=0

# ---------------------------------------------------------------------------
# WHAT THIS GATE IS EVIDENCE FOR, AND WHAT IT IS NOT.
# The six reds are the committed baseline in generated/gate/known_reds.txt --
# two enemy-AI probes and four sled legs, all older than this lane, untouched by
# it, and untouched by the road-repair landing it now sits on top of. EXACTLY the
# baseline, member for member, by the runner's own verdict: no new red, no fixed
# red, no count-matched-while-members-moved. Member for member it is the SAME set
# the dba79e46b run found, from a build that shares no CMake cache and no object
# file with it, and from a tree that now carries 186 files of main's road-repair
# code the earlier run had never seen.
#
# It proves the MERGED tree -- this lane's one-dial kernel rung ON TOP OF main
# ed0a43ce8 -- survives a from-clean reconfigure and rebuild, that main's +7 new
# legs are green here, and that the eight FACETCONTACT legs (L1..L8), the three
# CLIPPROBE legs and the T2b tape leg are all green on the shipped dial
# [ground] facet_contact 1.0.
#
# It does NOT grade:
#   * FACETCONTACT L6-STRICT -- hidden behind [.t3-strict], RED by measurement and
#     by design; Chad's ruling (T2 doc section 7) is still OWED, and hiding it is
#     what keeps a ruling of his from holding the gate hostage.
#     Run it deliberately:  build/seads_tests.exe "[.t3-strict]"
#   * FACETAI -- hidden behind [.facetai], a MANUAL leg (two 22-minute matches),
#     UNMOVED from T2b because its harness binds no BuildingColliders. The
#     AI-vs-buildings measurement does not exist and is named a GAP, not a pass.
#   * anything on a GPU, and anything about FEEL. Chad's fly word is recorded in
#     docs/terrain_clip/PREAUDIT_PACKET_*.md, docs/flight-log.md and LANES.toml,
#     not here.
#   * the Onaping/Valley pump apron -- HE DID NOT FLY IT, so nothing certifies it.
#   * road-repair's F2 junction flashing, which he reported on recon the same
#     evening ("looks fine except those flashing white lines at intersections").
#     That is another lane's rung and no leg here touches it.
