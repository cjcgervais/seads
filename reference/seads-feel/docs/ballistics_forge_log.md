# ballistics-forge ledger

One row per iteration. Fill iter/gate/golden/fable/keep by hand (the script writes E + the worst cell).

| iter | change | E | worst cell | gate | golden_moved | keep? |
|---|---|---|---|---|---|---|
| 0 | baseline: current vacuum integrator (no drag), truth PROVISIONAL | 1.1112 | vret@MG17_792:500m | green (unchanged) | no | baseline |
| 1 | RESEARCH: 4-gun fan-out + drag-fitted truth (MG17 pins the model <0.4% to a real Schußtafel) | 1.1870 | vret@MG151_20:500m | n/a (truth only) | no | truth frozen (Fable-vetted) |
| — | FABLE pre-consult: model CONFIRMED; drop=bore-referenced; **P0 pipper-fork** (shared-k mandatory); P1 air-relative launch speed; P2 SIE drop overshoot | 1.1870 | — | n/a | no | design vetted → implement |
| _pending_ | drag model landed | 0.0829 | drop@MG17_792:100m | _?_ | _?_ | _?_ |
| 2 | IMPLEMENT (Sonnet): quadratic drag in advance + pipper (shared k), exact-update, truth harness | 0.0829 | drop@MG17:100m (metric artifact) | 381/381 green | no | kept |
| — | FABLE red-team: **P0 C1** inherited-velocity fork (6.5m miss, moving shooter; cross-check is fixture-no-op); **P1 C2** discretization lag (1.28m@500m); **P1 C6** dragged-droop (aims high 0.85m@500m); P2 C3 stale tripwire, C4 drop floor, single-source risk | 0.0829 | — | n/a | no | fixing P0/P1 |
| _pending_ | P0/P1 fixes: world-frame pipper + lag + droop | 0.0431 | drop@MG151_20:200m | _?_ | _?_ | _?_ |
| 3 | FIX P0/P1 (Sonnet): world-frame pipper solve + discretization-lag + dragged-droop closed form; single-sourced k (main.cpp derives gunsight k from guns.cannon_drag_k); metric drop abs-floor (C4) | 0.0431 | drop@MG151_20:200m (within 8% tol) | 381/381 green | no | KEPT — CONVERGED |
|   | STATE: all reference cells within tolerance; moving-shooter deflection hits 7-14mm @300/400/500m (was 6.5m fork); all Fable P0/P1 resolved (P2 open: >800m scan-skip cosmetic, frozen-gravity 0.07m@500m, "hot k" = feel/Chad). NOT committed. | 0.0431 | — | — | — | → Chad fly |
| 4 | RENDER (Sonnet): wire battery in app (LMB->fire_held, &gw to step_frame), draw eye-relative tracer streaks (config colors/len, age-fade, additive), SEADS_SMOKE_FIRE hook | 0.0431 | (render pass; no metric change) | 381/381 green | no | KEPT — tracers VISIBLE (tracer_fwd/side.png verified) |
| 5 | POLISH tracers: DrawLine3D -> layered additive comet (soft glow + hot tapered core + white-hot head), view-distance sizing so bolts read at 20m and 500m; 20mm bigger/warmer than 7.92 | 0.0431 | (render polish; no metric change) | 381/381 green | no | KEPT — punchy (polish_fwd/side.png); prototype consts inline pending Chad fly then config-lift |
| — | FABLE pre-consult (harmonization): rise Δ=(0,+Δ,−conv) aim-point form; **single-source mandate** (hoist pipper's droop/TOF/lag lambdas → shared render/gunsight.h helpers, no fork); fixed body-frame +Y cant (attitude-independent, realistic); 3-pass fixed point; nominal = shooter-at-rest; **moved-golden** (convergence test oracle must fire the real drooped round, not re-record) | 0.0431 | — | n/a | no | design vetted → implement |
| 6 | HARMONIZATION (Sonnet): rounds cross the reticle/sightline at conv=300m. weapon::harmonization_rise (3-pass, shared drag laws) angles each gun up by its droop; muzzle_world_dir/spawn take g+fire_dt; drag laws hoisted to gunsight.h (expm1/log1p now ONCE each). Cannon Δ=0.963m (~3.2mrad), MG Δ=0.707m (~2.4mrad) | 0.0431 | (aim pass; drag/E unchanged) | 383/383 green (+2 new tests) | YES — convergence oracle re-derived (fires real round, ≤15mm up/5mm lat; catches no-comp ≈0.95m low) | KEPT — physics-verified; not flown yet |
| — | FABLE red-team (iter 6): **P0** pipper↔cant double-compensation — `lead_solution` raises aim by full droop AND guns physically cant up by Δ; nose-on-pipper lands ~3.24mrad HIGH (~1m@300m, 1.6m@500m); cross-check (`pipper_miss` test_guns.cpp:463) bypasses `weapon::spawn` so it's blind. **P1** no conv-range ceiling (conv=2000m ⇒ Δ≈797m, TOF>kMaxFlightTime). **P1** oracle at identity attitude only (body+Y==world-up ⇒ frame-confusion mutant invisible). **P2** dead re-forked TOF (gunsight.cpp:82-91 exp()-1 then void'd); oracle muzzle fixtures fork world.toml; g=0.0 default args; tight 15mm margin. Convergence/g=0-seam/single-source(live)/droop-math all held. VERDICT: boresight math sound, but pipper contract silently broken. | 0.0431 | — | n/a (review) | no | **P0 confirmed by author read → Chad ruling on sighting model** |
| — | CHAD flew iter 6: ruled **MODEL A** (Bf 109 Revi fixed-sight) — pipper becomes pure-lead (drop droop term, cant is sole droop compensator); fixed reticle sourced from gun boresight. Taste: **conv 300→500m**, **cannon drag 0.001565→0.00080** (arcade oomph, ~46%→~67% v@500m), tracers **orange/red + black slag**, lift inline tracer dials to config. | 0.0431 | — | n/a | no | design ruled → implement |
| 9 | HONEST PIPPER + FEEDBACK + TRUE-SCALE BANDITS (Sonnet; Chad ruled "both" + true-scale/labels/readout): (A) `lead_solution` restores droop via shared `dragged_droop_dist` MINUS cant rise `≈r·Δ/conv` (Δ from `harmonization_rise`, single-sourced) → no double-comp, reduces to today@500m, auto-holdover beyond; dead-astern pin (test #25) HITS 300/500/800/1000m (miss 0.28/0.47/0.75/**0.91m**, was 18m). (B) tracer_lifetime 1.5→3.5s (burns to impact), hitspark angular-sized, 3-state pipper GREEN/AMBER/RED (out_of_envelope=!valid∨|pred_miss|>hit_r). (C) drone size 4→1.0 true-scale, hit_radius 15→9m (true half-span+margin). (D) billboarded BANDIT labels (fade 1500→3000m). (E) live RNG+CLO closure readout (meter closure_rate). (F) debris double-advance fixed (draw.cpp base=f.pos), trigger-tap exploit fixed (cooldown drains not resets, ballistics.cpp:131), P0-2 spawn-safety assertion added to test 6. | — | — | 395/395 green (author-verified; +dead-astern pin) | YES — pipper y≈0→y>0.1 (droop restored), pipper-lead cross-check now passes conv/hub for cant-subtract, trigger-release golden replaced (drain not rearm); all re-derived w/ documented WHY | KEPT — honest-pipper pinned @4 ranges; single-source expm1/log1p ×1; NOT fly-checked (labels/scale/feel) |
| — | FABLE red-team (iter 8) + Chad live-bug (tail shot at range won't hit): root cause is NOT in combat/ — combat core VERIFIED solid (swept/two-pass/prev_pos/firewall-memcmp/determinism, 394/394 reproduced). **P0 system honesty gap:** model-A pure-lead pipper compensates NO droop + cant zeroes droop only @500m → dead-astern rounds sink 18m low @1000m (past 15m sphere) = permanent miss; INVISIBLE because tracers cull @1.5s (before impact) + drones drawn 4× oversize (1200m looks like 300m). Side shot hit = short-range crossing (<1m drop). **P1** explosion debris double-advances vel (draw.cpp:1196, detaches from fireball). **P2** trigger-tap exploit (ballistics.cpp:130 resets cooldown to 0 on release); hitspark fixed 2m + 15m sphere < 40m drawn span; overkill inflates cw.hits; a P0-2 spawn-safety assertion promised in a comment but absent from test body. Fixes offered: (A) keep pure-lead + tracer-to-impact/spark-scale/red-pipper-cue feedback; (B) cant-aware ballistic-honest pipper (auto-holdover, reduces to today @500m). | — | — | n/a (review) | no | **Chad decision: sight path A vs B; then Sonnet fixes** |
| 8 | KILL-LOOP (Sonnet, Fable-vetted design): new pure `combat/kill.h` — `combat_tick(pool, drones, cw, ap, dp, dt)` after fire_tick in step_frame (nullable cw ⇒ bit-identical). `Projectile.prev_pos` + `damage` single-sourced in weapon (P0-1). Drone `hp` + `respawn_in_place` helper (hp resets on kill+crash, P0-4). Swept seg-vs-moving-sphere relative-frame collision, smallest-s victim/tie→lower-idx; two-pass resolve (P0-2); 15m rendered-scale radius (P0-3). Age-driven clock-free FxPool (spark 0.2s + explosion 1.5s + 8 Fibonacci debris, P3c copy-at-spawn). HUD kills counter + 2s flash (ticks_since_kill, no player-clock). hp=100/cannon=30(4 hits)/mg=6. +11 pin tests. | — | — | **394/394 green (author-verified: 383+11; firewall memcmp + determinism both pass)** | no | KEPT — firewall/determinism/no-tunnel pinned; renders clean; explosion LOOK not fly-checked yet |
| — | KILL-LOOP thread OPENED (Chad ruled: medium tankiness / kill-FX-now / pre-consult-first). FABLE pre-consult: design SOUND w/ 4 design-P0s caught pre-build — **P0-1** `prev_pos` must live in `weapon` (spawn/advance), NOT caller snapshot (fire_tick reuses free slots → phantom-segment instakill); **P0-2** two-pass resolve (damage vs pre-kill segments, then kills) so same-tick burst can't wound a reborn drone; **P0-3** hit_radius tracks RENDERED scale (~15m; drone drawn size=4×span), not physical span (else tracers pass through visible wings); **P0-4** one `respawn_in_place` helper, hp resets on kill AND crash paths. Vetted: `combat/kill.h` pure module post-advance; swept seg-vs-moving-sphere (relative frame); hp=100/cannon=30(4 hits)/mg=6 flat; age-driven clock-free FX pool (spark+explosion, P3c copy-at-spawn); instant respawn; 10-test pin plan (incl firewall memcmp + determinism). | — | — | n/a (design) | no | design vetted → implement |
| 7 | IMPLEMENT MODEL A (Sonnet): P0 fix — `lead_solution` droop term removed (`I = p + v_tgt·t`, pure-lead); fixed cyan boresight reticle single-sourced from orient·(0,0,−1); cross-check re-pointed through `weapon::spawn` (real canted gun, blind→sighted); conv=500m; cannon k=0.00080; tracers orange/red comet + deterministic sin² slag, 15 tracer dials lifted to [guns]. P1: rolled-attitude frame oracle + conv-TOF envelope guard. P2: dead t_cap_mono deleted, g=0.0 default args removed, oracle fixtures aligned to world.toml. | 0.0431 (aim/render; drag arcade retune, E not re-measured) | — | 383/383 green (author-verified; Sonnet misreported 380) | YES — pipper cross-check re-pointed at spawn + test_gunsight "gravity raises aim" now asserts y≈0 (pure-lead); both re-derived w/ documented intent, NOT re-recorded | KEPT — P0 fix author-verified (droop term genuinely gone gunsight.cpp:110; single-source expm1/log1p ×1 each); slag rhythm + gunnery feel NOT flown yet |
| — | FABLE consults (×2, in-conversation; NOT the older firing-mechanism packet): KE damage law form (`\|v_rel\|²/v_ref²` range-falloff, `cosθ` obliquity at the SAME best_s, 2× overspeed-merge cap) + `fx_curves.h` closed forms (golden-angle/Fibonacci lattice, drag-consistent λ=0.10, exact burnout to 0). | — | — | n/a | no | design vetted → landed in iter 10 |
| 10 | KE DAMAGE + WHITE/RED PIPPER + ALL-BANDIT RANGE + HIT FX + CATCHABLE BANDITS (Sonnet; Chad ruled "full ballistics" polish then "put distance on ALL bandits — too fast to catch"): (A) real KE `dmg = D_ref·min(\|v_rel\|²/v_ref²,2)·clamp(cosθ,0.3,1)` — range-falloff (drag-slowed vel) + obliquity + 2× overspeed cap; `Projectile.damage` REINTERPRETED as REFERENCE dmg, new `v_ref`=muzzle speed set in `spawn`; cannon 30 / mg 6 KEPT (~4 point-blank cannon hits kill). (B) lead diamond WHITE off-solution → snaps RED on `gunsight_on_target` (replaces green/amber/red 3-state; out_of_envelope folds to white). (C) per-bandit slant range under EVERY in-front bandit (green fine print, m→km auto, NEVER fades — fixes the chased-bandit-vanishes gap); BANDIT tag fade 1500/3000→4000/9000 m; engaged-only readout removed. (D) new pure `combat/fx_curves.h` — closed-form spark/fireball/debris in (seed,i,age,energy01). (E) bandit cruise 140→115 m/s (`scenario.toml`+`drone.h`) so player gets ~50 m/s cruise closure; loader floor clears. | — | — | 400/400 green | YES — `test_load_scenario` re-pinned 140→115; damage helper re-pinned to set `v_ref` (center hit still deals exactly `damage`); +4 KE-physics + +4 fx-curve cases; all re-derived w/ documented intent | KEPT — committed `54cf6502d`; NOT fly-checked (white→red snap feel, green range clutter at large fleets, KE 30/6 balance under k=0.00080, spark/fireball look, 115 m/s chase feel) |
| 11 | BANDIT CATCHABILITY: cruise 115→95 m/s (Chad fly "still cant catch em"); ~72 m/s cruise closure, loader floor ~68 clears. Two `test_load_scenario` pins re-derived 115→95. | — | — | 421/421 green | YES — the two config-value pins follow the intended speed (not a behavioral golden) | KEPT — audio pass also in-tree (gun/explosion SFX layers, gate 421) |
| 12 | PIPPER DISPLAY FIX (parallax) — Fable-derived, math-proven pre-build. Root cause was NOT the solver (verified correct) but the DRAW: the diamond used `project_dir` (direction at infinity) while the bandit is an eye-projected finite point, so the eye's 9 m offset above the gun line floated it 23-39 mrad high, swinging with bank. Fix: draw the lead diamond at the WORLD IMPACT POINT `B = tgt_interp.pos + tgt_interp.vel·ttl` projected FROM THE EYE (new `DrawInfo::gunsight_lead_point`, set in main.cpp from the interpolated engaged drone; `draw.cpp` projects `B−eye`). Now it sits ahead of the bandit by the true lead (never on him — Chad sees his bank), parallax baked in; tracers flow into it; RED cue unchanged. Render-only (gate blind). | — | — | 421/421 green | no (render-only; no golden touched) | KEPT — Fable hit-proof 0.47 m@300 / 0.56 m@450 (8 m sphere); PENDING Chad's eye |
| — | DEFERRED (Fable-specified, careful fold w/ test re-derivation next): sub-metre lead-SOLVER accuracy folds — P2-4 cant along BODY-up (bank-dependent, up to 2.5 m @450/60°, biggest), P2-1 cant on relative dragged distance (~0.3 m), P2-2 launch from muzzle not CG (~0.27 m), P2-3 droop half-step (~0.02 m). Touch `lead_solution` → may move exact-value pinned tests, so NOT rushed. | — | — | n/a | no | pending — do with test re-derivation |
| — | FABLE MATH DIAGNOSIS — "pipper floats above the crossing bandit" (Chad fly; "figure it out with math, not testing"). Ported `lead_solution`+`dragged_*`+`harmonization_rise`+`weapon::advance` to a numeric scratch and ran the concrete 95 m/s beam-crosser @300/450 m. **VERDICT: NO-BUG — pipper math CORRECT.** A real `weapon::spawn` round fired down `lead_dir` HITS the crosser to **0.49 m @300 m / 0.60 m @450 m** (8 m sphere) — pipper↔round do NOT fork. Correct solution is 7.7° (40 m) AHEAD along track + **1.1 m BELOW** the LOS (cant over-covers droop inside conv). The "above" is 100% CHASE-CAM PARALLAX: both reticles project as directions-at-infinity but the eye sits 9 m above the gun line → a 300 m bandit reads ~22 mrad (~6.7 m) below the pipper wings-level, and banking rotates the 134-mrad horizontal lead into screen-up. Fable P2s LOGGED (sub-metre, deferred): cant charged on world-lead-dist vs relative traj (+0.23 m low); solver launches from CG not the +2.87 m muzzle (+0.42 m along-track); droop omits the semi-implicit ½-step; cant subtracted along world-up not body-up (2.2 m @90° bank). | — | — | n/a (diagnosis) | no | **Chad decision: pipper DISPLAY/UX fix (math is fine) — awaiting his aiming-model ruling** |
| 13 | DAMAGE MODEL CONFIG-DRIVEN (Session-4 follow-up): the component damage model's 17 feel/floor dials (combat::DamageParams — severity→effect, controllability floors, component_hp, hit-routing extents) lifted from hardcoded struct defaults to a `[damage]` scenario.toml section. Parsed + strictly validated in load_scenario.{h,cpp} (divisors > 0, the four controllability floors in (0,1], cd0_cap_mult >= 1, severity losses in [0,1], routing extents bounded); wired `cw.damage_params = scen.damage` in main.cpp. Committed toml values MIRROR the struct defaults exactly, so the config path is bit-identical to before (and damage_zero still short-circuits the whole transform at zero damage). Unblocks Chad tuning the hurt-plane feel with no recompile (the Session-4 ⓯ pending item). | — | — | 456/456 green (+4: defaults-match-struct pin, component_hp=0 / floor=0 / cd0_cap<1 rejections) | no (defaults mirror the old hardcoded values → no behavioral golden moved) | KEPT — build clean, app boots + parses [damage] (smoke ok); NOT committed |
| 14 | AUDIO SOFT-CLIP (Fable P2 fold, Session 2 deferred "tanh soft-clip on stacked booms"): both synth output buses (`GunSynth`, `CombatSfxSynth`) HARD-clipped (`if(s>1)s=1`) — a furball (many explosion+hit voices at once, now common with the bandit combat AI) overshoots ±1 and the corner distorts harshly. Replaced with a shared `render::soft_clip` (gun_audio.h): identity for `|s| ≤ kSoftClipThreshold=0.9` (single-sound timbre UNTOUCHED → below-knee samples bit-identical), then a C¹ tanh knee saturating smoothly to ±1 above it (slope 1 at the knee, no kink). Render-only, PURE, fixed-seed (firewall intact). | — | — | 460/460 green (+4: identity-below-knee, odd/monotone/bounded, ceiling-softened-not-pinned, stacked-furball bounded+audible) | no (soft-clip is identity below 0.9 → every existing audio golden bit-identical; only the overshoot region, which no test pins, changes) | KEPT — build clean; knee width tunable via `kSoftClipThreshold` (Chad's ear); NOT committed |
| — | FABLE AUDIT + pre-consult (Chad: "rounds feel too low — compensate for the small-planet non-euclidean geometry, pitch rounds away from the sphere in level flight"). Fable replicated `weapon::advance` bit-exactly and DEBUNKED the sphere attribution: v_muzzle(805) ≈ 2× v_orbital(√gr≈408), so a level round rides **+5.7 m HIGH** vs a co-altitude target @500 m — the sphere pushes rounds UP, not low. Real root cause = **velocity-inheritance × trim AoA**: the round inherits V along the flight path (γ below the nose), departing AoA-below the sightline → miss −1.5 m @cruise, **−3.8 m dragging 85 m/s, −5.3 m @3 G** at 500 m (low everywhere, worst where Chad fights; the S-dampff "felt-ask = missing term" pattern). Also caught **P1-1**: the pipper's droop cant is subtracted along WORLD-up but the gun cants in BODY +Y → banked shots fork 1.3 m @30°/3.6 m @90°. Recommended **C6c**: velocity-aware harmonization off = −(conv/v)·v_perp_body (+Δg), ONE `harmonization_offset` shared by round+pipper (kills the fork + fixes P1-1). Rejected C6a (orbital-hold is nose-DOWN 13.6 mrad, wrong sign) and C6b (dip-cover breaks reticle-on-target +7.35 m). | — | — | n/a (audit) | no | **Chad ruled: self-scaling only (no taste dial)** |
| 15 | VELOCITY-AWARE HARMONIZATION (Fable C6c; the "rounds too low" fix, self-scaling per Chad). New pure `weapon::harmonization_offset` = perp-to-nose body aim offset (Δg gravity rise + inheritance-cancel −(conv/muzzle_speed)·v_perp_body), consumed by BOTH `muzzle_world_dir` (round) AND `lead_solution` (pipper cant, now a BODY-frame VECTOR rotated by attitude → **fixes P1-1**) → single-sourced, cannot fork. Self-scaling +4.5 mrad @cruise → +13 mrad @3 G. Gated on g>0 (vacuum seam) + rest seam (v_body=0 ⇒ off=(0,Δg,0)) ⇒ every existing g=0/rest golden bit-identical. Folded P2 doc fixes (stale k=0.001565→0.00080) + predicted_miss range-consistency. +2 tests: (T1) inheritance fires — round elevation +0.00438 rad vs analytic 0.00436, sign both ways; (T2/T3) moving-shooter fires REAL canted gun on the pipper, HITS 0.97/1.08/1.05 m at 0°/60°/90° bank (mutation: world-up scalar cant → 2.5-3.6 m @bank → banked legs fail). | 0.0431 (aim pass; drag/E untouched) | n/a (aim) | **463/463 green** | **NO — rest+vacuum seams keep every existing golden bit-identical; the fix fires only in the untested moving+gravity regime, pinned by the 2 new tests** | KEPT — physics-verified + no-fork proven; NOT flown (awaits Chad's stick: does the higher stream read right) |
| — | FABLE RED-TEAM (iter 15, landed code, author≠reviewer). **NO P0.** Core CONFIRMED: sign (V1 — departure-velocity cancel, `s=conv/v` is the exact scale, no regime inverts), no-fork (V2 — both sites call the same function with single-sourced hub args), **no double-count (V3 — the world-frame solve owns muzzle→path, the cant owns nose→muzzle; orthogonal; the ~1 m floor is the 2nd-order velocity-ratio term, not a 1st-order double-count**), seams (V4 bit-identical), frame rotation (V5), tests genuinely pin the mutations (V6). **P1 FIXED same session:** `predicted_miss` folded the inheritance cant into its miss metric → a hard-G pull fired a FALSE red out-of-envelope pipper (|cue|≈12 m) though the shot HITS ≤1.5 m — the cue lied exactly when pulling lead. Fix: the cue uses only the gravity Δg of the cant; the aim solve keeps the full body-frame cant. +T4 test (hard pull v_body.y=−20: predicted_miss −0.79 m, out_of_envelope=false, shot connects). **P2 DEFERRED** (repo policy, careful fold w/ test re-derivation — same batch as the forge-log P2-1/2/4): (a) pipper cant linear scale omits `× m/a_final` → the dominant ~0.84 m of the ~1 m floor (rounds land that much LOW of the pipper, 1/5 the original complaint); (b) `gunsight muzzle_speed` (scenario.toml) vs `guns cannon_speed_mps` (world.toml) are two unenforced config sources now feeding the cant scale → add a loader equality check. | — | — | **464/464 green** (+T4) | no (Δg-only cue is bit-identical at rest; no existing golden pins moving+conv predicted_miss) | KEPT — red-team clean (no P0, P1 folded); ready to commit + integrate to main |
