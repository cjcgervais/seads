# v10 SEAL — Golden Felt Flight #4 fly card (2026-07-30)

Seal: tag `flight-kernel-v10-2026-07-30` @ `f86ee7b9f` (kernel commits `e22807a04`
lean_gain 8 + `3dfc8e44b` pool-ball retirement carry 0; rung F backported at
`ab63bf923`). Fly the GRAFTED recon **build-play** exe after the graft lands and
its gate reports green — the graft replaces the logged test flip with the committed
state, so what you fly is bit-for-bit the sealed kernel.

## NUMBERING (your question, answered from the ledger)
This is genuinely **Golden Felt Flight #4**. Three goldens are promoted (#1 v5,
#2 v6, #3 v9). At the v9 seal you flew TWO recordings: `felt_flight_3` was the
false-alarm test — sig-verified, deliberately NOT promoted, never a golden — and
`felt_flight_4` (the file) was promoted as Golden **#3** (the golden). File
counter ≠ golden number; nothing was renamed.

## CARD — one flight, two segments, F9 the whole thing

**Segment A — the butter (the #4 purpose):** ≥30 s of slow, smooth, small-deflection
tracking — the reconciliation feel you approved today. Gentle curves, tiny
corrections, some 2–5° work, a few moderate 5–10° holds. This is the regression
tape for exactly the butter this session bought.
- STATED PREDICATE (pinned at sealing, constants from your own sick-state tape):
  sub-5°-error full-reversal rate — sick state measured 6.24/s rudder, 3.27/s
  elevator; the sealed tape's number is measured at promotion and becomes the
  threshold. "Feels smooth" is not the record; the number is.

**Segment B — the pitch-down set (deliberate known-bad):** several
pitch-up-then-straight-down entries, some with a small off-angle. **These WILL
invert sometimes — that is by design.** The inversions are the documented
horizon-gate gap (aim above the world horizon from a climb; both push arms fail
to arm), taped as that thread's pinned BEFORE-state for its future A/B.
- KNOWN-LIMITS line (goes in the telemetry doc verbatim): the inversions on this
  tape are the horizon-gate gap's baseline evidence, NOT approved behavior.

After the flight: your verdict in your words; the docs agent runs the #4 sealing
checklist (signature verify, SHA-256, pin-derived telemetry with stated constants,
predicate-recovered counts, four-file convention, VERSIONS.md).

## Queued behind the seal (plan mode, one at a time, your rulings first)
1. The 5–10° blend-boundary roll slam (the bounce — FINE↔MANEUVER roll-target gap).
2. The horizon-gate nose-referenced arm (the inversions — designs on top of rung F,
   opens with Segment B's tape as its baseline).
