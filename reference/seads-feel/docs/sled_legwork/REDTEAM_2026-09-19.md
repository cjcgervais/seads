# SLED KERNEL N1 + N2 — THE RED-TEAM AND ITS FOLD (2026-09-19)

Lane `feel/sled-legwork-n1n2` in `D:\seads_sandboxes\sled-legwork`, off main `4acac47a2` (sled kernel v2).
Red-teamed tip: `04e06b182` (N2 `d1c6ace3e` + N1 `9e0cddfdc` + their gate records). Two independent
red-team passes produced eighteen findings (the JSON packet in the fold task, reproduced in full in §2).
Fold law: **every P0/P1 folded on the lane; P2/P3 recorded here, not folded** (the task's word). No v2
default moved; both dials stay one-dial (`ice_bite_mu` 0.0 identity / 0.25 toml; `leg_work_nm` 0.0
identity / 2400 toml). Fly tree `D:\flight_sim2\seads-recon` never opened.

**Fold commit:** see §4 (the SHA is written there after the commit). **Gate:** §5.

---

## §1 — WHAT WAS FOLDED (the four P1s), in kernel bytes and in test bytes

### F1 — the rolled-latch gate (P1-11, and it closes P2-2 with it)

`sim/sled.cpp` leg block, one expression:

```cpp
const bool gated =
    hands_on && contact && s.right_assist_armed && s.rolled;
```

`s.rolled` is the kernel's own readout (`sim/sled.cpp:285`: tilt > 75 deg HELD `rolled_persist_s` in
ground contact, hysteretic through `rolled_hold_s`), the same fact that makes R legal
(`app/player_mode.h:394 autoright_legal` needs `sled_rolled` when afoot). **The legs are legal exactly
where R is.** The PITCHED band read `|up_body.z|` against RADIAL up, so an upright machine on a >= 50 deg
bank was "on its end" to the stage.

Measured on the red-team's own probe (`scratchpad/rt_probe.cpp`, relinked read-only against the lane's
folded `build/lib*.a` as `rt_probe_fold.exe`; SF1 gaussian ridge h 25.5 sx 12 sy 40 = 52.2 deg flank,
depth 0.5, shipped toml, machine coasts in at 1.5 m/s facing uphill, throttle 0, one CTRL rising edge at
2.0 s held 1.0 s):

| arm | stage at press | leg_nm peak | omega peak [rad/s] | air peak [s] | tilt max [deg] | inverted | rolled | first divergence vs identity |
|---|---|---|---|---|---|---|---|---|
| pre-fold `04e06b182` (red-team's run) | 1 ARMED | 2049 | 11.00 | 1.23 | 179.8 | 1 | 1 | tick 240 (the press) |
| **post-fold** | **0** | **0** | **0.65** | **0.24** | **55.0** | **0** | **0** | **-1 (never)** |
| identity (either build) | 0 | 0 | 0.65 | 0.24 | 55.0 | 0 | 0 | — |

Trace at the press, post-fold: `t 2.00 | gs 0.29 tilt 51.8 ub(-0.00,0.62,-0.79) stage 0 armed 1 gs_lp 0.43
leg_nm 0 ... R 0` — the speed gate is open (`armed 1`), the band would read PITCHED (|z| 0.79), and the
rolled latch is what holds it.

What did NOT move (the fixtures the block was measured on both sit past 75 deg with R latched, `[N1 rest]`
prints `R` at every 0.5 s sample): stage 1 `off its end at 0.73 s after 1 press ... final tilt 150.4 ...
omega_peak 1.94` (== pre-fold digits); stage 2 `upright 1.43 s, |z| peak 0.39, omega_peak 3.38` vs
identity `1.62 s, 0.12` (== pre-fold digits); `rt_probe inv {0,2400,4000}`: omega 4.98 / 3.37 / 3.13,
t_right 1.61 / 1.42 / 1.35, leg_nm peak 0 / 1991 / 3577 (== the red-team's table). `rt_probe wheelie 2.0
1.0` still reads `stage seen 1` (the pre-existing kernel stands the machine on its tail at gs 3.7 and it
stalls there — a real nose-up-on-track, rolled latches, legitimately PITCHED).

Arithmetic consequence, stated in the code comment: rolled (|y| <= cos 75.06 = 0.2578) AND not on a side
(|x| < sin 35 = 0.5736) gives z² > 1 − 0.0665 − 0.329 = 0.6045, |z| > 0.7775 > sin 50 = 0.7660. **The
PITCHED enter threshold is now implied by the gate**; it stays as the band's stated shape, its exit (sin
40) is the hysteresis that still matters, and mutation M10 (`kLegPitchEnter = 0.0`) is a no-op on every
reachable state (§3).

### F2 — the stage-1 landing band pinned (P1-12)

`test/unit/test_sled_legwork.cpp` `legwork_ctrl_from_nose_up_kicks_it_off_its_end_within_6_s`, two lines
after `REQUIRE(r.presses == 1)`:

```cpp
REQUIRE(inverted(r.s));
REQUIRE(!upright(r.s, p));
```

Mutation M8 (`dir_x ? 1.0` -> `? -1.0`, the recon spec's toward-level) ran against the folded leg via the
red-team's `run_mut.sh` (post-fold `sled.cpp` re-backed-up first, sha `4a91161e...a0d2`): **1 failed of 10
assertions, `test_sled_legwork.cpp:545: REQUIRE( inverted(r.s) )` false** — the mutant prints `off its end
at 1.25 s after 1 press(es); final tilt 14.1 ub(-0.00,0.97,-0.24) side=0 upright=1; omega_peak 2.94`.
KILLED (it survived on `04e06b182`). Restored, `same-as-backup`.

The ruling itself is NOT taken here (it is his seat's, §2 item 12): the shipped sign stays **+1 backward**
(his word, run 5: "roll it over backward and on its side"; run 7's ladder wants the on-its-back rung), and
the ruling text with both measurements is in `sim/sled.h` next to the dial and in
`docs/SLED_KERNEL_N1_LEGWORK.md` §7.

### F3 — the bank leg (P1-13)

New `legwork_never_arms_on_an_upright_machine_on_a_bank` (`[sled][n1][legwork]`, ctest -N 2164 -> 2165).
Flat / 46.0 deg / 52.2 deg SF1 ridge flank (the rt_probe fixture ported: `world::SnowpackField::hill`
injected on a flat planet, `hill.up (0,0,1)`, peaks[0] = {0, 0, h, 12, 40, 0}, h 0 / 20.5 / 25.5),
machine spawned on the surface normal facing uphill at 1.5 m/s, throttle 0, coasts to rest; CTRL rising
edge at 2.0 s (held 1.0 s), SHIFT rising edge at 4.0 s (held 1.0 s), 6 s. REQUIREs: gs at the CTRL edge <
`right_assist_max_ms` (the speed gate is open, so it is not what holds the stage), tilt at the edge < 75
(upright to the kernel) and >= 45 on the flanks (really banked), `leg_stage == kLegNone` through the CTRL
press, `leg_nm_now` 0 over the run, and 4000 vs 0 whole-state (`SLEDTAPE_PIN_D` + surface + rolled)
identical on all 720 ticks. Printed:

```
[N1 bank flat]         slope  0.0 deg: at the CTRL edge gs 0.53 tilt  3.0; stage max through CTRL 0 (whole run 0) rolled ticks  0 leg_nm peak 0 omega peak 0.34 air peak 0.00; 4000 vs 0 first divergence tick -1; end tilt  2.5 gs 0.00
[N1 bank 46 deg flank] slope 46.0 deg: at the CTRL edge gs 0.40 tilt 45.5; stage max through CTRL 0 (whole run 0) rolled ticks  0 leg_nm peak 0 omega peak 1.14 air peak 0.20; 4000 vs 0 first divergence tick -1; end tilt 47.5 gs 0.00
[N1 bank 52 deg flank] slope 52.2 deg: at the CTRL edge gs 0.29 tilt 51.8; stage max through CTRL 0 (whole run 0) rolled ticks 55 leg_nm peak 0 omega peak 4.96 air peak 0.40; 4000 vs 0 first divergence tick -1; end tilt 83.0 gs 3.02
```

The 52-deg row's `rolled ticks 55 / omega 4.96 / end tilt 83.0` is the **SHIFT** edge firing the v2
**pendulum** (P2-15, carried: `w_tilt` reads 51.8 deg of pitch as tilt) — identically in both arms
(divergence -1, leg_nm 0). The leg says so in its comment and reads the stage REQUIRE through the CTRL
press only, so it pins the legs' claim and not the pendulum's. On `04e06b182` this leg would have failed
at the 52-deg row (stage ARMED, leg_nm 2049, divergence at the press: F1's table).

### F4 — the v2 pendulum-alone righting pinned (P1-1)

`legwork_shift_from_inverted_goes_on_end_then_rights`, identity arm `b`:

```cpp
REQUIRE(b.t_right > 0.0);
REQUIRE(b.t_right <= 12.0);
```

`b.t_right` = 1.62 s (printed `identity: on end -1.00 s, upright 1.62 s, |z| peak 0.12`). The two
`test_sled_selfright.cpp` inversion legs (`:318`, `:365`) read `shipped()` = 2400 since `9e0cddfdc` and
so measure pendulum + legs; this is now the only line that reds if the pendulum alone stops righting the
flat inverted fixture.

### Comment truths folded with the bytes

`sim/sled.h` `leg_work_nm`: the gate list now names the rolled latch; the ruling owed on the sign with both
measurements; the eight-dial coupling stated (P2-3 / P2-14). `app/main.cpp` `SEADS_SLED_LEGWORK` comment
said "CTRL kicks it toward level" — it ships backward; corrected.

---

## §2 — EVERY FINDING, VERBATIM SEVERITY, AND WHAT WAS DONE

| # | sev | rung | finding (short) | disposition |
|---|---|---|---|---|
| 1 | **P1** | N1 | v2 signed pendulum-alone righting from full inversion pinned nowhere at the shipped table (`b.t_right` printed, never REQUIREd) | **FOLDED F4** |
| 2 | P2 | N1 | PITCHED read against radial up, not the ground normal; slow machine on any >= 50 deg bank arms; CTRL fires 2400 N m with throttle open | **closed by F1** (same defect as #11). Throttle-open half: a rolled machine is already under v2's `rolled_throttle_frac` 0.15 cut, so the gate also brings the throttle in. Recorded for his drive: nose-down in a bank with the throttle on is drive question (ii) |
| 3 | P2 | N1 | eight pendulum dials silently shared; "killing the pendulum kills the legs" | **RECORDED**, coupling stated in `sim/sled.h`. ⚠ The evidence's last step is wrong and #14 supersedes it: `right_assist_armed` defaults **true** (`sim/sled.h:1543`), so with `right_assist_nm = 0` it is frozen TRUE — the legs are speed-UNgated, not dead. The CHECK the fix asked for was not added (P2 = record only) |
| 4 | P2 | N1+N2 | env band [0,0.45] / [0,4000] vs loader band [0,1] / >= 0 disagree; a toml line the env refuses ships | **RECORDED**. The loader comment (`config/load_scenario.cpp:621`) states the asymmetry as deliberate (a toml walk-back is never refused by the loader). Ruling owed if 0.45 is meant to hold at the loader |
| 5 | P3 | N1 | `gate_n1_legwork.log` predates the committed bytes | RESOLVED by the red-team's own rerun `build/gate_redteam_04e06b182.log` (6/2164, baseline six by name, exit 0); superseded by this fold's gate (§5) |
| 6 | P3 | N1+N2 | 17-digit goldens' PRE-edit provenance is a sentence, not a file | **RECORDED**: the pre-edit printout `n1_golden_pre.txt` (untracked, lane root) is reproduced in §2.1 below so the provenance is in a committed file. The seven-tape replay remains the proof that holds |
| 7 | P3 | N1 | tape_9's shipped-value divergence correctly attributed; R unavailable in the pre-latch PITCHED window | **RECORDED** in `SLED_KERNEL_N1_LEGWORK.md` §3.2: band INVERTED (`up_body.y` -0.92 at the edge), band entered ~tick 3236, dwell 36 ticks = 0.30 s, SHIFT rising edge tick 3282. ⚠ After F1 the ticks-3190..3208 window (on the end, `rolled 0`) is covered by NEITHER R nor the legs until the latch fills at 3210 — 0.15 s; the legs then cover it 0.30 s later. Accepted: the latch is the kernel's own word for "stuck" |
| 8 | P3 | N1 | ON_SIDE readout stage has no hysteresis (enter == exit sin 50); HUD blinks near 50 deg | **RECORDED**. Not folded. F1 puts the rolled latch (its own `rolled_hold_s` hysteresis) in front of it, which damps but does not remove the sin-50 flicker. No torque path |
| 9 | P3 | N1+N2 | probe-only replay overrides read the game's env names; older readers refuse tapes that carry `# cparam ice_bite_mu / leg_work_nm` | **RECORDED**. v2 precedent, same shape; the fly-tree probe/exe is rebuilt with the pull. This fold's replays `unset` both env names explicitly |
| 10 | P3 | lane | lane not in `LANES.toml` | **RECORDED**; owed before the push, not this fold's (no push here) |
| 11 | **P1** | N1 | PITCHED arms on a machine parked/crawling UPRIGHT on a >= 50 deg bank; CTRL backflips it down the hill | **FOLDED F1** (+ F3 pins it) |
| 12 | **P1** | N1 | the "backward" sign is contradicted at the shipped budget (toward-level lands upright in one press) and the stage-1 leg cannot tell the signs apart | **FOLDED F2** (band pinned, M8 killed). **RULING OWED TO HIS SEAT**, both numbers in `sim/sled.h` and the N1 doc §7: +1 backward (ships) -> on its BACK, tilt 150.4, omega 1.94, then SHIFT; -1 toward-level -> UPRIGHT, tilt 13.1, omega 3.43, no second rung. Note F1 alone already removes the bank tumble that made toward-level look safer (#11's table: post-fold omega 0.65 == identity with the shipped sign) |
| 13 | **P1** | N1 | no leg observes the attitude band (M10 `kLegPitchEnter = 0.0` survives the three riding/gating legs) | **FOLDED F3**. Honest ledger: M10 STILL SURVIVES the new leg (`17 assertions` pass) because after F1 the enter threshold is unreachable-dead (F1's arithmetic); the defect M10 stood for (an upright banked machine arming) is what F3 kills — on `04e06b182` F3 reds at the 52-deg row |
| 14 | P2 | N1 | the legs' speed gate is borrowed state advanced only inside the pendulum's `right_assist_nm > 0.0` branch; frozen TRUE with the pendulum killed by toml | **RECORDED** in `sim/sled.h` (leg_work_nm > 0 assumes right_assist_nm > 0). F1's rolled latch still holds the legs to a machine past 75 deg in contact in that case. No loader check added (P2) |
| 15 | P2 | v2 pendulum (carried) | SHIFT on a machine pitched >= ~20 deg but upright fires the pendulum's full roll torque (`w_tilt = acos(up_body.y)` reads pitch as tilt); the parked 52-deg machine tumbles on SHIFT in both arms | **RECORDED for the pendulum's owner**, now visible in the suite: F3's 52-deg row (`rolled ticks 55, omega 4.96, air 0.40`, divergence -1). Not N1's bytes; not a landing blocker |
| 16 | P3 | N2 | `sled_ice_bite_zero_is_the_identity`'s "KILLED BY: anything but a > 0.0 BRANCH" overstates: M1a (drop the guard) is bit-exact (+0.0); M1c (slip_ref x1.01) is killed | **RECORDED**. Comment-truth owed on the N2 comment (`test_sled.cpp:5640`, `sled.h`): the branch buys readability and `w_lo`'s exact zero; the golden watches the shipped lateral expression, not the guard. Not edited (P3) |
| 17 | P3 | N1 | mutation ledger: 9 kills, 3 survivors (M1a, M8, M10), 1 not mutable | **RECORDED** + updated after the fold: M8 KILLED (F2); M10 survives, unreachable (F1); M1a survives (#16). Tree restored after each, `same-as-backup` |
| 18 | P3 | N2+N1 | clean results: ice bite makes no rollover and no high-speed grip; the inverted lift LOWERS peak angular rate; the rider stays welded through every stage | **RECORDED** (numbers in the packet: icehook |a_lat| 0.308/0.357/0.442 g at 0/0.25/0.45, rolled 0; inv omega 4.98/3.37/3.13; grip 1 everywhere) |

### 2.1 The pre-edit golden printout (finding 6), `n1_golden_pre.txt`, verbatim

Printed by `seads_tests.exe "legwork golden printer"` on lane tip `301a48c37` + the N1 constants only,
before the leg block existed (2026-09-22); these are the literals in
`legwork_identity_zero_is_bit_exact_on_every_stage_fixture`:

```
[N1 golden nose-down CTRL] pos 3678298.7389791072 3678298.7390284152 3678299.9631969314  vel -0.00031952825859103695 0.0023226865530599225 0.00077159886624527415  q 0.35755074859336594 -0.13640261650655305 -0.32930008283267914 0.86319942298224506  w 0.00034705549124358648 1.4954514794094471e-05 -0.0067634524821750825  tilt 173.50
[N1 golden nose-up CTRL] pos 3678299.2687780871 3678299.2687759358 3678299.0572449588  vel 0.0020256724948841433 0.00099345017223695527 -0.0025981536967300957  q -0.21902991953339945 0.31380139820869429 0.75758959892364475 -0.52878405463305345  w 0.005388764910527896 -0.00021073336404295697 0.0089170975095718436  tilt 105.09
[N1 golden inverted SHIFT] pos 3678297.9555506543 3678300.2336918307 3678298.7518427712  vel 0.00080220946837912738 0.001154827622902457 0.0030503807003511379  q 0.21107239480337825 0.39918562653008094 0.86506235562306888 0.21855525756114966  w -0.0040565397985987831 6.5646009687280485e-06 -0.0010691155793042027  tilt 2.19
```

---

## §3 — THE FOLD'S OWN PROOF (identity unchanged, bytes)

Build: Ninja, Debug, the lane's `build/` (`ninja seads_tests seads_sled_probe`, 0 errors).

**Unit legs** (`seads_tests.exe "[legwork],[icebite],[selfright],[tape],[sled_tape]"`): `All tests passed
(295 assertions in 25 test cases)` before the leg narrowing; after it and after the mutation restores +
clean rebuild, `"[legwork],[icebite]"` -> `All tests passed (176 assertions in 13 test cases)`, `"[legwork]"`
-> `137 assertions in 9 test cases`.

**Golden replay at the identity** (`unset SEADS_SLED_ICE_BITE SEADS_SLED_LEGWORK`, the red-team's
`replay_rt.sh` on the folded `build/seads_sled_probe.exe`, out `replay_fold/`): the seven tapes
(`D:\flight_sim2\seads-recon\build-play\sled_tape_86..91.sledtape` read-only +
`D:\seads_sandboxes\sled-ride-b1\sled_tape_9.sledtape`):

| tape | filtered diff lines vs `replay_pre/` (v2 base, both dials absent) | raw diff lines vs `replay_post_n1/` (the N1 commit's own identity replay) | replayed | exit |
|---|---|---|---|---|
| 86 | 0 | 0 | 11430/11430 | 0 |
| 87 | 0 | 0 | 14272/14272 | 0 |
| 88 | 0 | 0 | 1837/11896 (pre-existing tick 46589) | 1 |
| 89 | 0 | 0 | 6355/15256 (pre-existing tick 68315) | 1 |
| 90 | 0 | 0 | 12990/12990 | 0 |
| 91 | 0 | 0 | 14833/20734 (pre-existing tick 14832) | 1 |
| tape_9 | 0 | 0 | 3983/20890 (pre-existing tick 3982) | 1 |

Filter, as N1 §3: `grep -v -E "DIAL GAP|dial\(s\)|dial gap|^    [a-z_]"`. **Every diff prints nothing.**

**tape_9 at the shipped 2400** (`SEADS_SLED_LEGWORK=2400`, probe-only override, `replay_fold_2400/`):
`replayed 3283/20890 ticks`, ground key mismatch at sample 220900, `rolled tape=3210 replay=3210` —
identical to the N1 commit's `replay_post_n1_default/tape_9.txt` (first divergence tick 3282: his SHIFT
edge fired the INVERTED stage; the rolled latch was already up at 3210, so F1 changes nothing there).

**Mutations on the fold** (`run_mut.sh`, post-fold backup sha `4a91161ecd8359723f994f024896f018ff03d420529dea2ec4960dcc2e09a0d2`):

| id | mutation | before fold | after fold |
|---|---|---|---|
| M8 | stage-1 sign `? 1.0` -> `? -1.0` | survived | **KILLED**: `legwork_ctrl_from_nose_up...` 1 failed / 10 (`:545 REQUIRE( inverted(r.s) )`) |
| M10 | `kLegPitchEnter = 0.0` | survived | survives (`legwork_never_arms...` 17/17, `press_gated` 6/6, `tuck` 3/3) — unreachable after F1, §1 F1 arithmetic |

Restored after each (`restored ... OK`, `same-as-backup`); `seads_tests.exe` rebuilt from the clean source
before any number above §3's unit-legs line was taken.

---

## §4 — THE FOLD COMMIT

Files: `sim/sled.cpp` (F1 + comment), `sim/sled.h` (comment: gate list, coupling, sign ruling),
`app/main.cpp` (env_dial comment truth only), `test/unit/test_sled_legwork.cpp` (F2, F3, F4),
`docs/SLED_KERNEL_N1_LEGWORK.md` (§1 gates, §3.2 tape_9 line, §4 the new leg, §7 the sign ruling), this
file. R tripwire: `git diff 4acac47a2 -- app/main.cpp app/player_mode.h | grep -c KEY_R` = 0 (R untouched).

SHA: recorded in §5's table with the gate.

---

## §5 — GATE ON THE FOLD TIP

| | |
|---|---|
| fold commit (§4) | `c8a61c05c1a6e07be8adc845dcde436d4addc6b8` (`c8a61c05c`), 2026-09-22 10:47:24 -0700 |
| tree at gate | clean (`git status --porcelain` tracked = 0 lines) |
| build | `ninja -C build` on the tip, 0 errors, `[13/13] Linking CXX executable seads.exe` |
| command | `ctest --test-dir build -C Debug -j4 --output-on-failure > build\gate_c8a61c05c.log 2>&1` — DETACHED (`Start-Process cmd.exe /c ...`, `-WindowStyle Hidden`), polled every 15 s from a scratchpad script; one ctest only |
| launched / log mtime | 10:48:43 / **11:05:52** (+18m28s after the commit, so the log is this tip's) |
| `Total Test time (real)` | **1028.76 sec** |
| ctest -N | **2165** (= 2164 + the F3 leg `#963 legwork_never_arms_on_an_upright_machine_on_a_bank`) |
| `.status` file | reads `0` — NOT ctest's exit: `cmd /c "... & echo %ERRORLEVEL%"` expands the variable at parse time. The verdict below is the runner's, not this file's |

```
99% tests passed, 6 tests failed out of 2165
```

`python tools/gate/gate_baseline.py check build/gate_c8a61c05c.log` (exit 0):

```
gate: 6 failed of 2165
baseline: 6 known reds

OK -- the red set is EXACTLY the baseline, member for member.
```

The six BY NAME: `79 probe P-F: the relentless raider keeps the pump and shoots back`, `119 E12.1: the
raider backfill keeps a faction's pump offense alive`, `1301 sled_slides_before_it_tips_on_flat_snow`,
`1302 sled_grip_ceiling_stays_below_the_tip_threshold`, `1340 sled_assist_reference_plane_is_load_weighted`,
`1343 sled_debug_sink_is_write_only` — the same six as `build/gate_redteam_04e06b182.log`,
`gate_n2_icebite.log` and v2's `gate_3990cde63.log`. **Nothing new.**

The folded legs in the gate: `#958 legwork_ctrl_from_nose_up_kicks_it_off_its_end_within_6_s Passed 2.01
sec` (F2 pin), `#959 legwork_shift_from_inverted_goes_on_end_then_rights Passed 0.89 sec` (F4 pin), `#963
legwork_never_arms_on_an_upright_machine_on_a_bank Passed 2.07 sec` (F3). The twelve `selfright*` legs at
`shipped()` (now behind the rolled gate as well) stay green.

**Unresolved after this fold (none are landing blockers; all recorded in §2):** the sign ruling (#12) is
his seat's; the pendulum's `w_tilt`-reads-pitch tumble (#15) is v2 bytes for the pendulum's owner; the
frozen-true speed gate when the pendulum is toml-killed (#14); env/loader band asymmetry (#4); ON_SIDE
flicker (#8); the N2 comment overstatement (#16); `LANES.toml` line (#10) before the push.
