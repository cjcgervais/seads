# PROVENANCE — `EvCTAPE-v3_20260803T2049_d5e0717-dirty.log`

**Written by:** the eagle, 2026-08-03, **on Chad's instruction** (*"flying_architecture should also
get the tape"*). **This is outside the eagle's write authority** — see §5. It is a file copy plus
this note into an empty directory, and it is reverted by deleting both.

**Read this cold.** `architecture` cannot read `D:/mandalark-kernel_sandbox_eagle`, so the eagle's
own packet on this tape is invisible to it. Everything needed to check the claims is restated here.

---

## 1. WHAT THIS FILE IS

A **Roblox Studio log**, copied verbatim. It is the tape. There is no `.csv` and there never was —
see §4.

| | |
|---|---|
| sha256 | `B85C43BBF6110AADED4FF8ACAED5AD9B99C4337BA092AA1BF7C19213E664F726` |
| size | 1,920.6 KB |
| origin | `%LOCALAPPDATA%\Roblox\logs\0.732.0.7321043_20260804T034854Z_Studio_CD233_last.log` |
| flown | 2026-08-03, ~20:49–20:51 local (Studio session `CD233`) |
| copied | 2026-08-03 ~21:00, **while Studio still held the source open** |

The source was live and locked when copied. Integrity was checked by re-counting frame rows
against the locked original (`5173` in both) and by byte length, **not** by hashing both — the OS
refused to hash the locked file. If you want a hash-to-hash comparison, take it once Studio is
closed and the source still exists.

## 2. THE TAPE'S OWN HEADER, TRANSCRIBED

```
[EvCTAPE] h,schema=EvCTAPE-v3,build=d5e0717-dirty 2026-08-03 14:41
          tree=EvC2026_sandbox_cascade branch=cascade/rebuild,cols=n,t,dt,spd,aoa,...
```

## 3. THE EAGLE'S CLAIMS — stated so they can be refuted

**These are claims, not findings. `G-10` is adversarial verification by an agent that did not
produce them, and this note exists to make that possible.**

| claim | measured | how to re-check |
|---|---|---|
| schema is `EvCTAPE-v3` | matches contract `C1` authority | the `h,` line above |
| 36 columns | matches contract `C2` authority | count the `cols=` list |
| 5,173 frame rows | | `grep -c '\[EvCTAPE\] r,'` |
| ordinals 2 → 5174, contiguous | **zero dropped frames** (`5174−2+1 = 5173`) | first/last `r,<ord>` |
| 86.19 s at 60.0 Hz | | col 2 (`t`) first vs last |
| 38 tokens on every row, none ragged | `r` + 36 cols + `#` | |
| **floor 1** — mouse deflection, no key held | **1,248 frames** | `mouseDx≠0 or mouseDy≠0` **and** `keyMask==0` (cols 32/33, 34) |
| **floor 2** — dwell servo fires | **4,711 frames** | any of `dwellT`/`dwellB`/`dwellD`/`dwellRamp` ≠ 0 (cols 21/22/23/25) |
| the two populations are separate | 1,823 any-mouse, 1,214 any-key | |

**The floor predicates are the eagle's reading of `G-1`'s requirement, not cascade-recorder's
implementation.** If `reconcile_rollout` or the recorder's own tooling defines either floor more
tightly, **its definition wins and these numbers should be recomputed.** A floor that passes only
under a loose reading is exactly the failure `G-1` was written to prevent.

## 4. WHY THERE IS NO `.csv` — do not go looking for one

`harness/legacy-replay/capture_server.ps1` carries an explicit **"DEAD — DO NOT USE. This server
has no producer"** banner. Nothing listens on `:8080` (verified at the time of the flight).
`_ScratchTele.server.luau` bails **quietly** when `HttpEnabled` is off, so a flight can appear to
succeed and post nothing.

> *"The only sink SOP-01 trusts is the Studio log itself: print → CreatorOutput → the log file,
> read directly."*

`captures/` standing empty next to a successful flight reads as failure and is not one.

## 5. ⚠ TWO DEFECTS THIS SURFACED — both for kernel-docs, neither fixed here

**(a) This write is outside the eagle's authority.** `agents.tsv` gives `captures/**` to `harness`
and the eagle only `D:/mandalark-kernel_sandbox_eagle::**`. The eagle wrote here anyway, on
Chad's explicit instruction, because the alternative was primary flight data expiring in a
rotating temp directory. **Recorded as an open deviation rather than performed quietly.** If the
graph's answer is that Chad's instruction routes through `harness`, then harness should re-land
these two files and the eagle's copy should be deleted.

**(b) `architecture` cannot read the eagle's tree, so `G-10` cannot cover `G-6` or `G-7`.**
`architecture`'s `reads` are `D:/mandalark-kernel::**`, `D:/EvC2026_sandbox_cascade::**`,
`D:/mandalark-cascade-research::**` — the eagle sandbox is absent. `G-10` requires *every*
criterion be adversarially verified by an agent that did not produce it, and two of the criteria
(`G-6` DONE, `G-7` direction settled) live entirely in a tree `architecture` is not permitted to
open. **That is a hole in `G-10`, not a nit.** Either `architecture` gains a read on
`D:/mandalark-kernel_sandbox_eagle`, or `G-10` is scoped to say it cannot cover the eagle. The
eagle's public remote, readable without any graph change, is
`https://github.com/cjcgervais/mandalark-kernel-sandbox-eagle`.

## 6. WHAT IS STILL OPEN ON THIS TAPE — cascade-recorder's calls, not settled here

- **86 s flown against a ~20 s dry-run script.** If the script specified a *sequence* with timing,
  this tape may be a free flight that happens to contain the right events rather than the scripted
  run. That distinction is load-bearing for `G-4`, where a tape with no control arm stays
  DESCRIPTIVE ONLY.
- **The build is `-dirty`.** `d5e0717-dirty` is a working tree with uncommitted changes, so this
  tape **is not attributable to a commit** and is reproducible only while that tree survives.
- **`G-2` has not been run.** `reconcile_rollout` on this tape (ord 36 vs ord 22 copy check) is
  the next step and belongs to cascade-recorder.

**No agent has claimed `G-1` on this tape.** The eagle verified the artefact and stopped there.
