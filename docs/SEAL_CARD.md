# Master Seal Card

**Seal:** ATM-Sphere v1.19r0
**Realm:** ATM-only
**Geometry:** R=15000 m, flattening=0
**Tick:** Δt=0.01 s (100 Hz)
**Gravity:** g₀=9.80665 m/s² (constant)
**Ceiling:** ATM_TOP=8000 m, SOFT=100 m
**Atmosphere:** still air; constant density ρ₀=1.225 kg/m³ (B1/B2 energy model; ISA-vs-altitude deferred to B5)
**Determinism:** det_math only; ban libm/fast-math/FMA/x87
**Wire/Hash:** GEO-001 (lat/lon×1e7, bearing×1e6, h×1e3; ZigZag+LEB128) + **KIN-002** aux block (phi×1e6, tas×1e3, **gamma×1e6**) + **WEAPON-001** aux block (per-aircraft **hp×1e3**, **fire_cd×1e3**, **ammo×1e0**, **last_hit_by×1e0**, **engine_hp/wing_hp/tail_hp×1e3**, **kills×1e0**; per live round: GeoPoint + **damage×1e3** + **ttl**/**owner** exact i64) — snapshot **protocol 7** (v1.12r0 added the gunnery state to the wire so MP can replicate rounds/HP/kills; **v1.14r0 added the magazine `ammo`** at unit scale so a remote client shows a rounds-remaining counter — protocol 4→5; **v1.17r0 added attacker attribution `last_hit_by`** at unit scale, ZigZag carrying the -1 "never hit" sign, so a remote client renders an attributed kill-feed — protocol 5→6; **v1.19r0 added the region sub-pools + kill tally** — pools milli like hp, kills unit like ammo — so a remote client draws the damage state and a scoreboard — protocol 6→7; throttle/target_g/fire are inputs, not state). The wire is lossy/downstream — the world_hash source of truth stays `Kernel::snapshot()`.
**Flight model:** B3 limits & stall (B2 lift & pitch) — full **3-DOF point mass**. Flight-path angle **γ is a stored state** (per-aircraft state is now the 7-tuple `lat,lon,psi,phi,alt,tas,gamma`), driven by commanded load factor `n` (`Command.target_g`). Altitude is **earned** (`alṫ = V·sin γ`); pulling g raises induced drag → bleeds speed. Generalizes B1 (`n=1/cosφ, γ=0` ⇒ the old `ψ̇=g₀tanφ/V` level turn). Builds on B1 energy (TAS from thrust−drag). V_MIN=30 m/s. **B3 (v1.7r0):** the achievable `n` is bounded per-airframe by BOTH the structural g limits (`n_min_struct`/`n_max_struct`) AND the C_Lmax aerodynamic ceiling `n_aero = cl_max*qS/(m*g0)` — `n = clamp(n_cmd, max(n_min_struct,-n_aero), min(n_max_struct,n_aero))`. Below the corner speed `V* = stall*sqrt(n_max_struct)` the aero ceiling binds and the turn collapses (**accelerated stall**); the 1 g stall speed falls out for free. Retires the B2 global placeholder [-3,9]; post-stall lift held at C_Lmax (no departure). No new det_math (det_sin/det_cos only). **B4 (v1.8r0):** **per-airframe aero retune (data-only mechanism, but moves goldens → reseal).** The envelopes carried the un-calibrated v1.2r0 aero, so emergent level top speeds ran ~30 % low (109–142 m/s). B4 lifts them to **historical** values (148–195 m/s ≈ 533–702 km/h) by moving **only `thrust_static_n`/`v_max_mps` per airframe** (solved via `tools/b4_retune.py`); every turn/stall-defining param (`mass`,`wing_area`,`cl_max`,`induced_k`,`n_max/min_struct`,`stall`, all LUTs) is **fixed**, so the iconic turn ordering (A6M2/Spitfire best, P-47 worst) and the B3 C_Lmax↔stall coherence are preserved exactly. `v_max_mps` is now the flat-curve thrust-zero asymptote (~700, not a physical Vne); `v_ne_mps` is validation-only (never loaded by the kernel). No new det_math, no state/wire change. Emergent perf table: `tools/perf_probe.py`.
**Weapons:** **Step 7 guns G1→G3 COMPLETE (v1.9r0 / v1.10r0 / v1.11r0).** A fired round is a deterministic point mass on the sphere — the **n=0 / thrust=0 specialization of the aircraft 3-DOF step** (gravity arcs γ down; lumped quadratic drag k=2e-4; heading fixed; integer ttl 250; despawns on ttl/ground). **G2 hit/damage:** a round within **HIT_RADIUS 60 m** (horizontal great-circle via the spherical **law of cosines** vs `cos(HIT_RADIUS/R)` — **acos monotone ⇒ no det_acos**) AND **HIT_ALT_GATE 60 m** of an **alive** enemy (≠ firer) deals damage and despawns; **hp≤0 == dead** ⇒ flight frozen, can't fire, excluded from hits (rounds pass through). **G3 per-airframe roster + fire-rate:** the gun globals moved into the envelope — **hp_start** (toughness), **muzzle_v_mps**, **damage_per_round** (carried by the round), **rof_interval_ticks** (fire-rate, gated by a per-aircraft **fire_cd** cooldown, decrement-then-fire). So .50-cal platforms (P-47D/P-51) = high rate + durable; 20-mm cannons (A6M2/La-7) = high damage/round; A6M2 = glass cannon. **NO new det_math** across all three (det_sin/det_cos + ÷×− only). Canonical snapshot now: aircraft **9×f64** (…,gamma,**hp**,**fire_cd**); projectile **7×f64** (…,gamma,**damage**) + u32 ttl + u32 owner. `Command.fire` is the trigger. Per-airframe constants live in the envelopes (data); drag/ttl + the no-arg START_HP stay global. **Wire transport (v1.12r0):** the gunnery state now rides the **WEAPON-001** snapshot section (protocol 3→4) — per-aircraft hp/fire_cd + the live rounds (position/damage/ttl/owner) — so multiplayer/replay can DRAW the dogfight from the wire (the `seads_record --gundemo` viewer sources HP bars + tracer rounds + kills from the decoded wire). Transport-only: hp/fire_cd/damage already live in the canonical state ⇒ **all 9 goldens byte-identical** across the reseal.
**Ammunition (G4, v1.13r0):** **Step 7 guns G4 COMPLETE — the gunnery model is closed.** Each envelope now carries **ammo_start** (the per-airframe magazine, abstract rounds). Firing is gated on **ammo > 0** as well as the fire-rate cooldown; a shot spawns the round, decrements ammo (clamped at 0), and resets the cooldown. An empty magazine falls silent (**"Winchester"**) — the trigger may stay held and the aircraft alive, but no further rounds spawn and the cooldown is not reset. **ammo is the 10th per-aircraft snapshot f64** (appended after fire_cd) ⇒ all 9 prior goldens moved (ammo constant/identical for the unchanged scenarios) + the NEW **GOLDEN-SK-Winchester-001** (an A6M2 empties its 100-round cannon at tick 891, then silent). Relative WWII endurance: A6M2 fewest (100, the ~60-rpg 20 mm cannon), P-47D most (340, eight .50-cal belts). **STILL no new det_math** (a pure integer-valued counter, like fire_cd); ammo lives in the CANONICAL state and — **since v1.14r0 — also rides the WEAPON-001 snapshot wire** (the 10th per-aircraft field, unit-scale quantized, snapshot protocol 4→5), so a remote/late-join client shows a rounds-remaining counter (exactly as fire_cd became kernel-state at G3 then rode the wire at v1.12r0). Transport-only reseal ⇒ all 10 goldens byte-identical. See ADR-Step7-Guns-G4 (counter) + ADR-Step7-Guns-WireTransport-Ammo-v1.14r0 (wire).
**Convergence (v1.15r0):** **gun harmonization.** Each envelope carries **convergence_m** (per-airframe boresight range). SEADS models a single **centerline** battery, so harmonization is realized as **vertical boresight zeroing** — a fired round's initial **γ is offset UP** by the flat-fire drop-compensation angle **δ = ½·g₀·convergence_m / v²** (v = firer TAS + muzzle_v) so its ballistic trajectory crosses the aim (sight) line at convergence_m instead of always falling below the nose. **Pure ±×÷ ⇒ NO new det_math.** A **kernel spawn-geometry** change (round initial γ) ⇒ moves the **3 firing goldens** (Gunfire/Hit/Winchester); the **7 non-firing goldens are byte-identical**. **No wire change** (γ already rides KIN-002 / the projectile block); session digest moves (SESSION-SK-001 fires), **event digest unchanged** (kill sequence identical). Per-airframe (~180–275 m): p47d 270, p51 275, spitfire/ki61 230, a6m2 250, la7 200, bf109/yak3 180. See ADR-Step7-Guns-Convergence-v1.15r0.
**Attribution (v1.16r0):** **attacker attribution — "who fired the killing round".** Each aircraft carries **last_hit_by** (the index of the aircraft whose round most recently damaged it, or **-1 == never hit**), set at hit time from the striking round's **owner** — the **kernel-side event hook** the earlier guns/netcode layers deferred (layer 6 could DERIVE hit/kill moments from hp deltas but not say WHO). It **persists through death**, so at hp≤0 it names the **killer** (an attributed kill-feed). **last_hit_by is the 11th per-aircraft snapshot f64** (appended after ammo) ⇒ **all 10 goldens move** (last_hit_by = -1 identical for every aircraft never hit; GOLDEN-SK-Hit-001's a6m2 now records last_hit_by = 0, the p47d) — **proven additive**: stripping the 11th f64 from each v1.16r0 golden reproduces its v1.15r0 hash byte-for-byte (attribution perturbs no trajectory). **STILL no new det_math** (a pure integer-valued state, like fire_cd/ammo). **Since v1.17r0 it also rides the WEAPON-001 wire** (the 11th per-aircraft field, unit-scale, snapshot protocol 5→6 — deferred one seal exactly as ammo was kernel-state at G4 then rode the wire at v1.14r0) **and stamps the layer-6 EVENT channel** (`Event.attacker`, the 7th event field — the server reads the target's post-step last_hit_by when it derives a hit/kill from the observed hp delta), so a remote client renders the attributed kill-feed from EITHER path (freshest state frame or exact-sequence event journal). Transport-only reseal ⇒ all 10 goldens byte-identical; session + event digests move. See ADR-Step7-Guns-Attribution-v1.16r0 (hook) + ADR-Step7-Guns-WireTransport-Attribution-v1.17r0 (wire).
**Region damage + kill tally (v1.18r0):** each airframe carries three functional **region sub-pools** beside the total hp — **ENGINE** (0.375×hp_start), **WING** (0.5×hp_start), **TAIL** (0.25×hp_start); global exact-binary fractions, **independent thresholds, NOT a partition** (a connecting round's damage books into the total hp AND the struck region, clamped at 0). The region is pure **approach aspect**: `rel = wrap_pi(round ψ − target ψ)` — |rel| < π/4 == astern → TAIL, |rel| > 3π/4 == head-on → ENGINE, else beam → WING. **wrap_pi + compares only ⇒ NO new det_math** (seventh consecutive zero-transcendental guns seal). A dead region degrades a **LIVING** aircraft: **engine out → thrust forced 0** (a decelerating glider); **wing out → n_aero HALVED** (half the lifting surface; binds below the corner speed like the B3 accelerated stall); **tail out → control authority lost** (commanded bank/g overridden to 0.0/1.0 — a straight 1-g mush; throttle untouched). hp≤0 death unchanged and dominates. **`kills`** is the per-aircraft victory tally: +1 on the ATTACKER exactly on the round that crossed the target hp>0→≤0 (the HitEvent `killed` round — the per-round queue makes shared-kill credit exact); persists through the attacker's own death. **engine_hp/wing_hp/tail_hp/kills are the 12th–15th per-aircraft snapshot f64s** ⇒ all 10 prior goldens move — **proven additive** (strip the 4 new f64s ⇒ each v1.17r0 hash byte-for-byte; the tail-out override is a NO-OP for a straight-and-level 1-g target, which every sealed victim is). GOLDEN-SK-Hit-001's p47d now reads **kills = 1**; NEW **GOLDEN-SK-EngineOut-001** (a head-on burst kills an A6M2's engine, not the aircraft; the survivor decelerates thrustless). `HitEvent` gains **region**; all four fields were **off the WEAPON-001 wire at v1.18r0** (deferred like ammo at G4 / last_hit_by at v1.16r0) and **joined it at v1.19r0** (snapshot protocol 6→7, transport-only: pools ×1e3 like hp, kills ×1e0 like ammo; all 11 goldens byte-identical; session digest `24f71845…`→`7e275f2b…` — the client view now surfaces the damage state + scoreboard; event digest `06629a69…` **unchanged**). See ADR-Step7-Guns-RegionDamage-v1.18r0 + ADR-Step7-Guns-WireTransport-RegionDamage-v1.19r0.
**Roster (8):** P-47D · Bf 109 F-4 · Ki-61 · A6M2 · Yak-3 · La-7 · Spitfire Mk V · P-51  — now with per-airframe armament + toughness + magazine (see ADR-Step7-Guns-G3 / G4).
**Goldens (11 total; C++ ≡ Python bit-for-bit GCC + Clang. v1.18r0 appended **engine_hp/wing_hp/tail_hp/kills** (region damage + kill tally) as the 12th–15th per-aircraft f64s ⇒ **ALL 10 PRIOR MOVE** — proven additive (strip the 4 new f64s ⇒ each v1.17r0 hash byte-for-byte; region pools full and kills = 0 wherever nothing is hit) + NEW EngineOut:**
- GOLDEN-SK-Sphere-001 — 10,000 ticks; (0°,0°), ψ=45°, TAS=250 m/s — `6914a994…2b13eb20` (**moved v1.18r0**: region pools + kills appended; all prior fields byte-identical)
- GOLDEN-SK-Turn-001 — ki61, level coordinated 45° turn, 6,000 ticks — `86e5cce3…a621efea`
- GOLDEN-SK-Climb-001 — bf109f4, pull-up into the ceiling soft band, 3,000 ticks — `8298b25b…60add3dd`
- GOLDEN-SK-TurnClimb-001 — spitfire_mk5, banked climbing pull, 4,000 ticks — `2dacd969…b2090f9b`
- GOLDEN-SK-Accel-001 — p51, wings-level throttle program, 5,000 ticks — `1b5af38c…cc2c10df`
- GOLDEN-SK-Pitch-001 — p51, zoom-climb then dive, 3,000 ticks — `f0925329…36b39ec4`
- GOLDEN-SK-Stall-001 — 2-ship V-n limit (ki61 accelerated stall + p47d g-limit zooms), 3,000 ticks — `027dbd60…ef9e9290`
- GOLDEN-SK-Gunfire-001 — **(G1, G3-armed)** 2-ship gunfire, p47d + a6m2 bursts (fire-rate gated), 300 ticks — `1c6f8417…fdf11d9e`
- GOLDEN-SK-Hit-001 — **(G2, G3-armed)** tail-chase **gun kill**: p47d (.50cal, 12 dmg, rof 3) downs a 70-HP a6m2; corpse freezes (records **last_hit_by = 0**; the a6m2's TAIL pool reads 0 — astern hits — and the p47d's tally reads **kills = 1**), spent rounds pass through, 200 ticks — `c0b4ecda…1edb1669` (**moved v1.18r0**: region + tally recorded on an unchanged trajectory)
- GOLDEN-SK-Winchester-001 — **(G4)** single-ship **ammunition depletion**: an a6m2 empties its 100-round 20 mm cannon at tick 891, then the gun falls silent (Winchester), 950 ticks — `c34ce0d5…900a7a1e` (depletion preserved)
- GOLDEN-SK-EngineOut-001 — **(v1.18r0)** head-on **engine kill on a SURVIVING aircraft**: a p47d 4-round burst connects head-on (every round → ENGINE region), the a6m2's engine pool drains 26.25→0 on the 3rd round while total hp stays 22 — the survivor decelerates thrustless (150→~131 m/s) in level 1-g flight, 700 ticks — `e9617633…fc3ba078` (NEW)

## Seal history
| Seal | Date | Change |
|------|------|--------|
| v1.19r0 | 2026-07-02 | **Step 7 guns — region pools + kill tally on the WEAPON-001 wire:** the v1.18r0 region sub-pools `engine_hp/wing_hp/tail_hp` (12th–14th canonical f64s) + the victory tally `kills` (15th) join the WEAPON-001 snapshot section as the 12th–15th per-aircraft fields (**snapshot protocol 6→7**) — pools quantized **milli (1e3, like hp** — quarter-integer values, so exact), kills at **unit scale (1e0**, an integer counter like ammo). New rail fields `wire.weapon.{enginehp,winghp,tailhp}_scale=1000` + `kills_scale=1`. Fifth instance of the proven wire-reseal pattern (KIN-001 v1.4r0, WEAPON-001 v1.12r0, ammo v1.14r0, last_hit_by v1.17r0): `snapshot_ref.py`↔`snapshot.{h,cpp}` extended (gated on protocol≥7; protocol-6 back-compat proven), 4 vector headers regenerated (snapshot/weapon/session/framing — framing only because its example payloads are default-protocol frames; the envelope codec is byte-unchanged), `test_weapon_wire.py`/`test_session.py` +2 (`test_protocol6_omits_regions`, region-damage-and-scoreboard-replicate-under-loss) ⇒ **166** property tests. **Bundled riders:** the session client-view surfaces the four fields (`FINAL_WEAPON` gains engine/wing/tail_milli + kills — the astern-killed A6M2 reads tail_milli=0 with engine/wing intact, the P-47 reads kills=1; digest `24f71845…`→`7e275f2b…`); `seads_record` carries them + emits a `"kills"` scoreboard array. **Event layer byte-identical** (unlike v1.17r0 — the Event record is untouched; digest `06629a69…` did not move). **TRANSPORT-ONLY — no kernel/det_math/tuning touched ⇒ ALL 11 GOLDENS BYTE-IDENTICAL**; no new golden, no new ctest target ⇒ guardian.yml unchanged. Rails version 280→290. **Region-damage arc closed end-to-end (kernel + wire).** See ADR-Step7-Guns-WireTransport-RegionDamage-v1.19r0. |
| v1.18r0 | 2026-07-02 | **Step 7 guns — region damage + kill tally:** each airframe gains ENGINE/WING/TAIL **region sub-pools** (0.375/0.5/0.25 × hp_start, exact-binary global fractions; independent thresholds beside the total hp) drained by approach aspect (`wrap_pi(round ψ − target ψ)`: astern → TAIL, head-on → ENGINE, beam → WING), plus **`kills`** (victory tally, +1 on the attacker per killing round). A dead region degrades a LIVING plane: engine out → T=0, wing out → n_aero halved, tail out → commands forced (0, 1 g). **NO new det_math** (wrap_pi + compares — seventh consecutive zero-transcendental guns seal). 12th–15th per-aircraft snapshot f64s ⇒ **all 10 goldens move, proven additive** (strip 4 f64s ⇒ v1.17r0 hashes byte-for-byte) + NEW **GOLDEN-SK-EngineOut-001** (head-on engine kill; survivor decelerates thrustless). Off-wire this seal (deferred like ammo/last_hit_by); session/event digests unchanged; only lockstep+predict vectors regenerated. guardian.yml gains the 11th golden. 164 property tests (+11). Rails 270→280. See ADR-Step7-Guns-RegionDamage-v1.18r0. |
| v1.1r1 | 2025-10 | Roster-8 sealed; ATM_TOP=6000 m |
| v1.2r0 | 2025-10-12 | ATM_TOP 6000→8000 m (+100 m soft band); kernel/wire unchanged |
| v1.2r0 (repo) | (Pass 1) | Deterministic core + harness stood up; golden sealed |
| v1.3r0 | 2026-06-28 | Envelope-driven flight inputs (bank/climb) + scripted-timeline goldens (Turn/Climb/TurnClimb). Sphere hash unchanged; no rail change. |
| v1.4r0 | 2026-06-28 | **Wire reseal:** auxiliary KIN-001 block (phi×1e6, tas×1e3) carries bank/speed for client-side prediction (netcode layer 4b); snapshot protocol 1→2 (KIN as a 2nd self-delimiting section). GEO-001 codec byte-unchanged; kernel/det_math/snapshot-layout untouched; **all 4 goldens unchanged**. |
| v1.5r0 | 2026-06-29 | **Flight model B1 (longitudinal energy):** TAS is now an integrated state (thrust − parasitic/induced drag − climb cost); `Command` gains throttle; per-airframe aero params added (constant ρ₀, V_MIN=30). No new det_math (uses +−×÷ and det_cos). Energy lives in `step(cmd,env)` only ⇒ **GOLDEN-SK-Sphere-001 unchanged**; Turn/Climb/TurnClimb **regenerated** + new **GOLDEN-SK-Accel-001**. Wire/snapshot layout unchanged (throttle is an input). See ADR-Step8-FlightModel-B1. |
| v1.6r0 | 2026-06-29 | **Flight model B2 (lift & pitch):** flight-path angle **γ promoted to stored state** (state 6-tuple → 7-tuple); full 3-DOF point mass driven by commanded load factor (`Command.target_climb`→`target_g`); altitude earned (`alṫ=V·sinγ`); pulling g bleeds speed. No new det_math (det_sin/det_cos). **Wire reseal KIN-001→KIN-002** (gamma×1e6; snapshot protocol 2→3). **ALL goldens regenerated** (incl. Sphere — γ joined the canonical snapshot; trajectory unchanged) + new **GOLDEN-SK-Pitch-001**. Generalizes B1's level turn. Vertical (cosγ→0) is a documented singularity; per-airframe C_Lmax/stall = B3. See ADR-Step8-FlightModel-B2. |
| v1.7r0 | 2026-06-29 | **Flight model B3 (limits & stall):** achievable load factor `n` now bounded per-airframe by the **structural g limits** (`n_max_struct`/`n_min_struct`) AND the **C_Lmax aerodynamic ceiling** (`n_aero=cl_max·qS/(m·g₀)`) — retires the B2 global [−3,9]. **Accelerated stall + corner speed emergent**; 1 g stall speed coherent with each envelope's `stall_tas_mps`. New envelope scalars `cl_max,n_max_struct,n_min_struct` (8 airframes). **No new det_math, no state-vector/wire change** (n is derived). Conservative extension ⇒ **the 6 prior goldens are byte-identical** (limits never bind in those scenarios); only **GOLDEN-SK-Stall-001** added. See ADR-Step8-FlightModel-B3. |
| v1.8r0 | 2026-06-29 | **Flight model B4 (per-airframe aero retune):** lifts emergent level top speeds from ~30 % low (109–142 m/s) to **historical** (148–195 m/s ≈ 533–702 km/h) by moving **only `thrust_static_n`/`v_max_mps` per airframe** (`tools/b4_retune.py`); all turn/stall params fixed ⇒ iconic turn ordering (A6M2/Spitfire best, P-47 worst) + B3 C_Lmax↔stall coherence preserved exactly. `v_max_mps` is now the flat-curve thrust-zero asymptote (~700, not Vne); `v_ne_mps` validation-only. **No new det_math, no state/wire change** — but the retune moves the **6 scenario goldens** (every scenario airframe retuned) so it is a reseal; **Sphere unchanged** (kinematic anchor). New tooling `perf_probe.py`/`b4_retune.py`; `test_energy` settle horizon 80→140 s (flatter curve). See ADR-Step8-FlightModel-B4. |
| v1.9r0 | 2026-06-30 | **Step 7 guns — G1 (ballistic projectiles):** fired rounds are deterministic point masses on the sphere = the **n=0/thrust=0 specialization** of the aircraft 3-DOF step (gravity along path + lumped quadratic drag; heading fixed; integer ttl; despawn on ttl/ground). `Command.fire` trigger; rounds are **canonical kernel state** (a **projectile block** appended to the snapshot). **No new det_math, no wire change** (KIN-002 unchanged; wire transport of rounds deferred). The appended block (n=0 for gun-less scenarios) moves **all 7 prior goldens incl. Sphere** (trajectories byte-identical) + new **GOLDEN-SK-Gunfire-001** (35 rounds airborne). 81 property tests (+9 `test_projectile.py`). Hit/damage = G2; per-airframe roster + fire-rate = G3. See ADR-Step7-Guns-G1. |
| v1.10r0 | 2026-06-30 | **Step 7 guns — G2 (hit detection + per-aircraft hitpoints):** a round within HIT_RADIUS 60 m (horizontal great-circle via the **law of cosines vs cos(HIT_RADIUS/R)** — acos monotone ⇒ **no det_acos**) AND HIT_ALT_GATE 60 m of an **alive** enemy (≠ firer) deals 25 hp and despawns. Each aircraft gains **hp** (start 100); **hp≤0 == dead** ⇒ flight frozen, can't fire, excluded from hit tests (rounds pass through the corpse). **No new det_math** (det_sin/det_cos only), **no wire change**. `hp` is the **8th per-aircraft snapshot f64** ⇒ all 8 prior goldens move (hp=100 identical) + new **GOLDEN-SK-Hit-001** (a P-47D tail-chase gun kill of an A6M2: 4 hits → 0 HP, corpse freezes). Projectile format unchanged (owner already carried). 88 property tests (+7 `test_hit.py`). Per-airframe HP/armament = G3. See ADR-Step7-Guns-G2. |
| v1.11r0 | 2026-06-30 | **Step 7 guns — G3 (per-airframe weapon roster + fire-rate):** the G1/G2 global gun constants move into each envelope — **hp_start** (toughness), **muzzle_v_mps**, **damage_per_round** (carried by the round), **rof_interval_ticks** (fire-rate, gated by a per-aircraft **fire_cd** cooldown, decrement-then-fire). Airframes now differ (P-47D durable/high-rate .50cal; A6M2 glass-cannon 20mm; etc. — initial balance pass). **No new det_math, no wire change.** `fire_cd` is the **9th per-aircraft snapshot f64** and `damage` the **7th projectile f64** ⇒ all 9 goldens move (Sphere fire_cd=0; Gunfire/Hit also change behaviorally — fire-rate gating + per-airframe HP). **No new golden** (Gunfire/Hit now exercise the roster) ⇒ guardian.yml unchanged. 94 property tests (+6: `test_weapon.py` roster + fire-rate/lethality in `test_hit.py`). **Guns arc G1→G3 COMPLETE.** See ADR-Step7-Guns-G3. |
| v1.13r0 | 2026-06-30 | **Step 7 guns — G4 (finite ammunition):** each envelope gains **ammo_start** (per-airframe magazine; loaded via envelopes.AERO_FIELDS). Firing is now gated on **ammo > 0** as well as the fire-rate cooldown; a shot consumes one round (ammo decremented, clamped at 0) and an empty magazine falls silent (**"Winchester"** — no spawn, no cooldown reset). **ammo is the 10th per-aircraft snapshot f64** (appended after fire_cd) ⇒ all 9 prior goldens move (ammo constant/identical; verified Sphere trajectory+hp+fire_cd byte-identical) + NEW **GOLDEN-SK-Winchester-001** (an A6M2 empties its 100-round cannon at tick 891). A6M2 fewest (100), P-47D most (340). **NO new det_math** (integer-valued counter, like fire_cd), **no wire change** (ammo off-wire, deferred like fire_cd was pre-v1.12r0 ⇒ WEAPON-001 parity byte-identical; session/event digests unchanged). Reseal because a golden world_hash changes. Rails version 220→230. **Guns arc G1→G4 COMPLETE.** 116 property tests (+3). See ADR-Step7-Guns-G4. |
| v1.17r0 | 2026-07-01 | **Step 7 guns — `last_hit_by` on the WEAPON-001 wire + `Event.attacker`:** the per-aircraft attacker index `last_hit_by` (the 11th canonical f64, kernel-only since v1.16r0) joins the WEAPON-001 snapshot section as an 11th per-aircraft field (**snapshot protocol 5→6**), quantized at **unit scale** (1e0 — an integer-valued index; ZigZag carries the -1 "never hit" sign natively). New rail field `wire.weapon.lasthitby_scale=1`. Fourth instance of the proven wire-reseal pattern (KIN-001 v1.4r0, WEAPON-001 v1.12r0, ammo v1.14r0): `snapshot_ref.py`↔`snapshot.{h,cpp}` extended (gated on protocol≥6; protocol-5 back-compat proven), all 4 vector headers regenerated, `test_weapon_wire.py`/`test_event.py` +2 (`test_protocol5_omits_lasthitby`, attribution-replicates) ⇒ **128** property tests. **Bundled riders:** the layer-6 EVENT channel gains **`Event.attacker`** (7th event field — the server stamps the target's post-step last_hit_by onto each derived hit/kill ⇒ an attributed kill-feed, "AC0 downed AC1"; EVENT + BLACKOUT digests move `dfcc1aaf…`→`06629a69…`, `94ae31ea…`→`90d6e67c…`); the session client-view surfaces last_hit_by (`FINAL_WEAPON` gains it — the dead A6M2 reads last_hit_by=0, the P-47; digest `fda717fe…`→`24f71845…`); `seads_record` carries it. **TRANSPORT-ONLY — no kernel/det_math/tuning touched ⇒ ALL 10 GOLDENS BYTE-IDENTICAL**; no new golden, no new ctest target ⇒ guardian.yml unchanged. Rails version 260→270. **Attribution arc closed end-to-end (hook + wire + events).** See ADR-Step7-Guns-WireTransport-Attribution-v1.17r0. |
| v1.16r0 | 2026-07-01 | **Step 7 guns — attacker attribution (last_hit_by):** each aircraft gains **last_hit_by** — the index of the aircraft whose round most recently damaged it (**-1 == never hit**), set at hit time from the striking round's **owner** (the round already carries owner for the G2 hit test). The **kernel-side event hook** the guns/netcode arc deferred: layer 6 could DERIVE hit/kill moments from hp deltas but not attribute them. Persists through death ⇒ at hp≤0 it names the **killer** (an attributed kill-feed). **last_hit_by is the 11th per-aircraft snapshot f64** (appended after ammo) ⇒ **all 10 goldens move** — **proven additive** (stripping the 11th f64 reproduces the v1.15r0 hash byte-for-byte; attribution perturbs no trajectory). GOLDEN-SK-Hit-001's a6m2 now records **last_hit_by = 0** (the p47d). **STILL no new det_math** (a pure integer-valued state, like fire_cd/ammo). **Off-wire this seal** (deferred like ammo was at G4): only `lockstep`/`predict` vectors move (they hash world_hash); snapshot/weapon/session/event vectors byte-identical. **No new golden / ctest target ⇒ guardian.yml unchanged.** Rails version 250→260. 126 property tests (+7). The wire transport + layer-6 Event.attacker are the v1.17r0 follow-up. See ADR-Step7-Guns-Attribution-v1.16r0. |
| v1.15r0 | 2026-06-30 | **Step 7 guns — gun convergence (boresight harmonization):** each envelope gains **convergence_m** (per-airframe boresight range; new AERO field). SEADS models a single **centerline** battery, so harmonization = **vertical boresight zeroing**: a fired round's initial **γ is offset UP** by the flat-fire drop-compensation angle **δ = ½·g₀·convergence_m / v²** (v = firer TAS + muzzle_v) so its trajectory crosses the aim line at convergence_m. **Pure ±×÷ — NO new det_math.** A **kernel spawn-geometry** change ⇒ only the **3 firing goldens move** (Gunfire `b25bb81c…`, Hit `8b8a84be…` — kill preserved, Winchester `14aa488b…` — depletion preserved); the **7 non-firing goldens are byte-identical** (they never fire) and keep their v1.13r0 stamp. **No wire change** (γ already on KIN-002/projectile block): weapon-vector parity untouched, session digest moves (SESSION-SK-001 fires), **event digest unchanged** (kill sequence identical). +2 property tests ⇒ 119. No new golden / ctest target ⇒ guardian.yml unchanged. Rails version 240→250. See ADR-Step7-Guns-Convergence-v1.15r0. |
| v1.14r0 | 2026-06-30 | **Step 7 guns — `ammo` on the WEAPON-001 wire:** the per-aircraft magazine `ammo` (the 10th canonical f64, previously kernel-only) joins the WEAPON-001 snapshot section as a 10th per-aircraft field (**snapshot protocol 4→5**), quantized at **unit scale** (1e0 — a pure integer counter, so exact AND compact, like ttl/owner). New rail field `wire.weapon.ammo_scale=1`. Third instance of the proven wire-reseal pattern (KIN-001 v1.4r0, WEAPON-001 v1.12r0): `snapshot_ref.py`↔`snapshot.{h,cpp}` extended (ammo gated on protocol≥5; protocol-4 back-compat proven), `gen_weapon_vectors.py`→`weapon_vectors.h`→`seads_weapon_test` regenerated, `test_weapon_wire.py` +1 (`test_protocol4_omits_ammo`) ⇒ **117** property tests. **Downstream riders (no-seal):** the session client-view now surfaces `ammo` (session digest moves; `seads_session_test`/`session_vectors.h` regenerated — the P-47 walks its magazine 340→333 over the burst while never-firing planes stay full); `seads_record` emits an `"ammo"` array for the web HUD. **Event layer byte-identical** (derives from hp deltas, not ammo). **TRANSPORT-ONLY — no kernel/det_math/tuning touched ⇒ ALL 10 GOLDENS BYTE-IDENTICAL**; no new golden, no new ctest target ⇒ guardian.yml unchanged. Wire reseal only (like v1.4r0/v1.12r0). Rails version 230→240. **Guns arc G1→G4 now fully wired (canonical + replicable).** See ADR-Step7-Guns-WireTransport-Ammo-v1.14r0. |
| v1.12r0 | 2026-06-30 | **Step 7 guns — weapon WIRE transport (WEAPON-001):** the gunnery state now rides the 20 Hz snapshot wire as a third self-delimiting section (snapshot **protocol 3→4**): per-aircraft **hp×1e3 + fire_cd×1e3**, then a projectile count and, per live round, a GeoPoint (bearing = heading) + **damage×1e3** + **ttl/owner** (integer counters carried EXACTLY, not quantized). New rail block `wire.weapon`. Mirrors the proven KIN reseals: `snapshot_ref.py`↔`snapshot.{h,cpp}` extended, new `gen_weapon_vectors.py`→`weapon_vectors.h`→`seads_weapon_test` byte-exact parity gate (13th receipt gate `weapon_codec`), `test_weapon_wire.py` (+6 ⇒ **100** property tests). **TRANSPORT-ONLY — no kernel/det_math touched, hp/fire_cd/damage already in the canonical state ⇒ ALL 9 GOLDENS BYTE-IDENTICAL** (no new golden; guardian.yml gains the weapon test leg only). Downstream rider (no seal needed but bundled): `seads_record` now sources the renderer's HP bars + tracer rounds + kills from the **decoded** WEAPON-001 wire (retiring the kernel side-channel) ⇒ `--gundemo` proves it end-to-end. Wire reseal only (like v1.4r0 KIN-001). See ADR-Step7-Guns-WireTransport. |
