# CARD 2 BUILD REPORT — eagle engineer → docs agent (S63, 2026-08-01)

**Card 2 is BUILT and SHIPPED to the stamp: EvC commit `9ac7c7a` (branch updraft, pushed).**
Pipeline per the ruled process, gate honored: your reconcile verdict's F1–F3 folded → ONE
red-team (BLOCK, 4 MAJORs, all folded into the design text) → ONE Sonnet build → Opus
verify (SHIP-WITH-FIXES) → fix round → Opus delta-verify **SHIP** (suite 659/659, zero new
analysis findings). Chad's ONE flight is pending; the fly card is the EvC HANDOFF S63 box.

For the eagle ledger, the items you'd want banked:

1. **Your three red-team handoffs, adjudicated:** H1 → the pass-table language was indeed
   insufficient; C2-1's pass is now `witness > 0 AND zero shallow ENTERs` via a denial
   witness that conjoins the S62 would-have-fired arm (fires exactly on the convicted
   elevDn ∈ [2,10) ∧ side ≤ 18 ∧ be ≥ 120 geometry). H2 → CLEARED as intended lift-vector
   physics; the φ_need normalize was REJECTED as the ruled-flaw error-gated shape. H3 → #5
   cleared structurally; #7's −G delegation exposed the split band's under-specification,
   now pinned as the two-sided corrEff selector.
2. **A real pole bug found and fixed** (Opus-adjudicated between two conflicting reads of
   HEAD): Stage-C's mid-flight `upPerp` fallback rebuilt the carried frame from
   `cf.UpVector` — body-roll contamination of the mouse basis near the pole; the concrete
   candidate for the registered R4 z-clamp residual and part of the straight-up veer class.
   Now an explicit zenith cone-hold (rotations structurally skipped, frame untouched).
3. **C2-4 predicate amendment — flagged for your spot-check as requested:** the instrument
   is stated as the EAGLE TRANSLATION of Golden-#5 (its envelope concept — committed
   entries, scoped population, stated window — carried over; the plane's near-LEVEL dip
   form cannot verbatim measure a straight-UP hold). Eagle form: departure from the entry
   maneuver plane, `|asin(look·planeN)|`, < 1.0°, free episodes (look.Y 0.985/0.98
   hysteresis) + a deterministic F6 leg; command-following error is a SEPARATE column,
   never folded in. If this translation drifts from the F2 fold's intent, say so before the
   flight is judged.
4. **Ledger lines:** (N4) the degenerate nose-vertical push hold has NO per-knob revert —
   backed out only by `aimPushMode=false` or the whole-card revert, deliberate. (N1) the F1
   row-#6 entry-gate mechanism claim was corrected: eagle `cosPhiTheta` is unsigned by
   construction; the inverted region is defended by the pre-existing uprightFade. (N5) the
   inverted-lateral-throw corner (asin bank form saturates at ±90°) is pre-registered on
   the fly card, lever named. D1b's revert is the PAIR to 0/0 (enter-only diverges —
   red-team MAJOR-2, boolean-traced by Opus).
5. **Stamp pre-registration:** fly only on `9ac7c7a`, clean. The lineHoldFF k-sweep remains
   Chad's post-card procedure, now non-null (FN-1 killed by E2, FN-2 by the knife decouple);
   kEff additionally fades by (1−pushW) — the recorded push-interaction decision.
