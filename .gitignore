# Build artifacts
/build/
/build-*/
/out/
*.obj
*.o
*.exe
*.dll
*.so
*.dylib
*.lib
*.a
*.pdb

# Golden run candidates (regenerated; only the sealed expected hash is tracked)
*.candidate.bin
/run_*.bin

# Flight recordings (binary; regenerated by seads_record). The web viewer's sample
# src/client/web/trajectory.js IS tracked so the page opens without a build.
*.seadsrec
/flight_*.seadsrec

# Python
__pycache__/
*.pyc
.venv/
.hypothesis/
.pytest_cache/

# Editor / OS
.vs/
.vscode/
*.user
.DS_Store
Thumbs.db
